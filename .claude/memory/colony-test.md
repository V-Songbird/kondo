---
name: colony-test
description: Colony smoke test. This memory exists only in the repo copy under .claude/memory
metadata:
  type: project
---

Auto memory for kondo is shared through the repo at `.claude/memory/` since 2026-09-04. If a session can read this file, `autoMemoryDirectory` in `.claude/settings.local.json` works. Delete this memory once confirmed.
