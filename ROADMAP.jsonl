{"foreman_roadmap_format":2}
{"id":"001","title":"Build the mutation journal and kondo trash","why":"ADR-0001 makes every mutation reversible, and nothing in the app writes yet. The journal and trash have to exist before the first toggle, not alongside it.","what":"Append-before-write journal at <kondo-data>/journal.jsonl, a kondo-owned trash at <kondo-data>/trash/<journal-id>/, and an undo(journalId) workspace method. Ships with the ADR-0001 safety-invariant tests docs/testing.md defers to the first mutation: journal written before the store is touched, undo restores a fixture byte-for-byte, no write lands outside a known store.","status":"done","source":"user","depends_on":[],"planned_touches":["electron/main/workspace/","shared/contract.ts","test/","docs/adr/"],"observed_touches":["CHANGELOG.md","docs/plans/001-mutation-journal-and-trash.md","docs/plans/README.md","docs/testing.md","test/boundary.test.ts","test/mutation.test.ts","test/workspace.test.ts"],"commits":[],"created_at":"2026-08-28","updated_at":"2026-08-29","notes":"2026-08-29 unverified: launch the app (npm run dev) and confirm the composition root resolves <kondo-data> to Electron userData (%APPDATA%/Kondo on Windows) rather than anything under ~/.claude. electron/main/index.ts now passes app.getPath(userData) into createLocator, and no test can construct the real Electron app, so this one line is proven only by types. Nothing is visible in the UI yet: check the resolved path, not a screen.\n2026-08-29 Shipped electron/main/workspace/mutations.ts. mutate() appends one JSON line to <kondo-data>/journal.jsonl and fsyncs it before any store byte moves, then runs the move/trash/write steps; a step names a store root plus a relative path and pathWithin refuses anything that escapes. Trash displaces into <kondo-data>/trash/<journal-id>/<store>/<relative>, never an unlink; the EXDEV fallback is copy, verify, then release. undo() reverses a whole multi-step operation in reverse order, is itself journaled with an undoOf link, and moves current bytes into its own trash before restoring displaced ones. Locator gained kondoDataRoot from Electron userData with KONDO_DATA_ROOT for fixtures. Seam gained journalList, journalUndo and trashSize plus JournalEntryInfo and TrashReport; no mutation channel ships, per the plan. Red-green proof: test/mutation.test.ts failed on a missing module before the code existed, went 12/12 green after, and swapping appendJournal/act inside mutate() turned 2 of the 12 red again (the ordering case and the failed-journal-write case); the swap was reverted. Checks run: npm run typecheck exits 0; npm run lint exits 0 (oxlint 1.80.0, confirmed live by a deliberate probe file that it did flag, then deleted); npm test 55/55 across 11 files; node .jig/checks/run.mjs reports no findings. Beyond the stated file surface I also updated docs/testing.md (its safety-invariant bullet still said these tests were deferred to the first mutation), docs/plans/001 (status plus a What actually shipped table of 4 divergences), docs/plans/README.md and CHANGELOG.md, because AGENTS.md treats a PR that leaves a doc wrong as incomplete. docs/adr/ needed no change: ADR-0001 already states this design, so no ADR was invented. Work is on branch worktree-foreman-001-mutation-journal in .claude/worktrees/foreman-001-mutation-journal. Two PostToolUse jig hook firings were false positives, quoting the src/ renderer rule and the test/ real-store rule at files under electron/ and test/; the same checks through the real driver report no findings.\n2026-08-29 scope drift — predicted but untouched: electron/main/workspace/, shared/contract.ts, docs/adr/; touched but unpredicted: CHANGELOG.md, docs/plans/001-mutation-journal-and-trash.md, docs/plans/README.md, docs/testing.md\n2026-08-29 lesson recorded: .foreman/notes.jsonl, area test\n2026-08-29 verified (was unverified): ran a headless Electron probe mirroring electron/main/index.ts — app.setName(Kondo) then app.getPath(userData). Result on this machine: kondoDataRoot = C:/Users/Songbird/AppData/Roaming/Kondo, Claude user store = C:/Users/Songbird/.claude, insideClaudeStore = false. ADR-0001 decision 6 holds at the composition root. No window was opened; npm run dev was not needed.","doc":"docs/plans/001-mutation-journal-and-trash.md","model":"opus"}
{"id":"002","title":"Introduce the kind registry and capability matrix","why":"foundations.md names this as v0.2's structural step. A single boolean write-permission model was enough for skills alone and does not survive five entity kinds.","what":"Replace the hard-coded adapter set with kinds that each supply discover, read, capabilities, enable and disable, with identity per ADR-0008. Write permission becomes a capability matrix keyed by kind, scope and operation rather than one flag. The snapshot cache, error isolation and id allow-list skeleton stay as they are.","status":"done","source":"user","depends_on":[],"planned_touches":["electron/main/workspace/","shared/contract.ts","docs/foundations.md"],"observed_touches":["CHANGELOG.md","docs/foundations.md","test/kinds.test.ts","electron/main/workspace/capabilities.ts","electron/main/workspace/desktop-store.ts","electron/main/workspace/kinds.ts","electron/main/workspace/mutations.ts","electron/main/workspace/sessions.ts","electron/main/workspace/user-store.ts","electron/main/workspace/workspace.ts","shared/contract.ts",".foreman/notes.jsonl","ROADMAP.jsonl"],"commits":["df159d4","87cf2a3","8816a8b"],"created_at":"2026-08-28","updated_at":"2026-08-29","notes":"2026-08-29 2026-08-29 Discrepancy with the brief: there was no single write-permission boolean to replace. Grepping writable/read-only/canWrite/permission/mutable across electron, shared, src and test returned only unrelated hits (`readonly` modifiers in mutations.ts and scan.ts, the string 'read-only preview' in src/app/app.tsx). SkillInfo.enabled is a state flag carried from Claude's own convention (ADR-0006), not a permission flag, and it stays as it was. The matrix is therefore introduced fresh rather than replacing a flag; nothing was removed. 2026-08-29 Shipped two new modules. electron/main/workspace/capabilities.ts holds the matrix: MATRIX is Record<EntityKind, Record<scope, Capabilities>> and capabilitiesFor(kind, scope) returns the row, with an unrecognized scope refusing both operations rather than throwing (ADR-0005). Rows encode Claude's own conventions per ADR-0006 - a user skill may be disabled and a user-disabled one enabled, a plugin-shipped skill neither, plugins toggle in any settings layer, and hooks refuse both because Claude has no per-hook disable convention. electron/main/workspace/kinds.ts holds the registry: seven entries (skill, plugin, hook, settings, project, session, desktopSession) each supplying discover, read, capabilities, enable and disable, plus createKindContext which memoizes the settings-layer and plugin-manifest reads per call. Code and desktop sessions are two entries sharing kind 'session' because they live in different stores; the matrix keys them apart by scope. enable/disable are seats returning null on every kind - the matrix says what is permitted, and no MutationPlan is built and no channel invokes one, per this entry's no-new-mutation-path rule. 2026-08-29 Invariants held. workspace.ts now names no adapter: grepping scanSkills|scanPlugins|hooksFromLayers|readSettingsLayers|desktopSessions|toSessionProjects|toSessionSummaries|summarizeTranscript across workspace.ts, ipc.ts and index.ts returns only the KondoApi method name desktopSessions, never the adapter. Every id-shape guard, badRequest and unknownId path in workspace.ts is byte-identical to before; only the line that picked an adapter changed. The lazy context preserves today's exact I/O and collector ordering - hooksList still reads only the settings layers, skillsList still reads projects then layers then the plugin manifest then the skill trees - which is why the read-path, boundary (ADR-0002) and mutation (ADR-0001) suites pass untouched. 2026-08-29 Seam: shared/contract.ts gained EntityKind, CapabilityOperation, CapabilityDecision, Capabilities and EntityIdentity, and SessionProject, SessionSummary, DesktopSession, SkillInfo, PluginInfo, HookInfo and SettingsLayerInfo now extend EntityIdentity, so every entity crosses the bridge carrying its kind and its matrix row. Each adapter stamps them where it builds the row, so the renderer never parses an id to learn what it may do. No new channel and no preload change: JournalEntryInfo.kind and MutationPlan.kind were tightened from string to EntityKind now that the vocabulary is defined. 2026-08-29 Red-green proof: flipping MATRIX skill/user/disable from ALLOW to a denial turned test/kinds.test.ts red at the 'keys permission on kind, scope and operation' assertion (1 failed, 8 passed); reverting returned it to 9/9. 2026-08-29 Checks run: npm run typecheck exits 0 (also with --force, a full rebuild); npm run lint exits 0 (oxlint 1.80.0, silent); npm test 64/64 across 12 files, up from 55/11, with every pre-existing test unchanged; node .jig/checks/run.mjs reports no findings. Two PostToolUse jig hook firings were false positives, quoting the raw-path-across-the-seam guard at electron/main/workspace/workspace.ts and the renderer-purity guard at test/kinds.test.ts - both guards are scoped to electron/preload, electron/main/ipc.ts and src/ respectively, and the real driver reports nothing. 2026-08-29 Scope drift - predicted and touched: electron/main/workspace/, shared/contract.ts, docs/foundations.md; touched but unpredicted: test/kinds.test.ts and CHANGELOG.md. The test file is this task's own coverage (docs/testing.md requires adapter-level tests and AGENTS.md makes all three checks a completion bar); the changelog bullet is required by docs/README.md, which treats a PR that leaves a doc wrong as incomplete. No docs/plans/ file was written: docs/plans/README.md says a plan is the first commit of a feature, not a document written afterwards. 2026-08-29 Work is on branch foreman/002-kind-registry-and-capability-matrix, created from main; there is no git remote on this repo.\n2026-08-29 scope drift — predicted but untouched: electron/main/workspace/, shared/contract.ts; touched but unpredicted: CHANGELOG.md, test/kinds.test.ts\n2026-08-29 lesson recorded: .foreman/notes.jsonl, area .\n2026-08-29 scope drift — predicted but untouched: docs/foundations.md\n2026-08-29 scope drift — predicted but untouched: electron/main/workspace/, shared/contract.ts; touched but unpredicted: .foreman/notes.jsonl, CHANGELOG.md, ROADMAP.jsonl, test/kinds.test.ts\n2026-08-29 scope drift — touched but unpredicted: .foreman/notes.jsonl, CHANGELOG.md, ROADMAP.jsonl, test/kinds.test.ts","model":"opus"}
{"id":"003","title":"Enable and disable skills through the native convention","why":"ADR-0006: kondo speaks Claude's own language rather than keeping shadow state. Disabling a skill means moving it to skills.disabled, which Claude already honours.","what":"Move a user-scope skill between ~/.claude/skills and ~/.claude/skills.disabled through the journal, with the scoped equivalent for project skills. Plugin-shipped skills are read-only and the capability matrix must say so. The UI gets a toggle per skill row that re-reads the store after every mutation.","status":"done","source":"user","depends_on":["001","002"],"planned_touches":["electron/main/workspace/","src/features/skills/","shared/contract.ts","test/"],"observed_touches":["CHANGELOG.md","docs/domain.md","docs/testing.md","electron/main/ipc.ts","electron/main/workspace/capabilities.ts","electron/main/workspace/kinds.ts","electron/main/workspace/mutations.ts","electron/main/workspace/user-store.ts","electron/main/workspace/workspace.ts","electron/preload/index.ts","shared/contract.ts","src/features/skills/skills.tsx","test/helpers.ts","test/kinds.test.ts","test/mutation.test.ts","test/skill-toggle.test.ts",".foreman/notes.jsonl","ROADMAP.jsonl"],"commits":["50ea668","9e3f64c"],"created_at":"2026-08-28","updated_at":"2026-08-29","notes":"2026-08-29 unverified: run npm run dev, open the Skills tab, and look at the new toggle column. Check that a global skill shows a Disable button that flips the row to the disabled pill after one click without a manual refresh, that a plugin-shipped skill shows a greyed-out Enable whose tooltip reads the matrix reason (Plugin-shipped skills follow their plugin - toggle the plugin instead), and that a refusal renders as the red card above the table. Every one of these is proven headlessly in test/skill-toggle.test.ts; what no command can settle is how the button reads and whether the list visibly re-reads rather than flickering.\n2026-08-29 Shipped kondo's first user-facing mutation. A skill toggle is one journaled move step between skills and skills.disabled in the skill's own scope; no second write path exists and nothing is unlinked. kinds.ts gained skillPlacement/skillPlan, replacing the skill entry's noPlanYet seat: the plan is built only when entity.capabilities[operation].allowed, so the matrix from 002 is the gate and a refused operation never reaches a MutationPlan. workspace.skillToggle validates the id shape and the operation, resolves the skill through kinds.skill.read, and on a null plan pushes the matrix's own reason as a new not-permitted ScanError rather than inventing prose. Seam additions: skillToggle(skillId, operation) plus its channel, ScanErrorCode not-permitted, SkillScope project-disabled. No new per-skill capability field was needed - see discrepancy 1. THREE DISCREPANCIES WITH THE BRIEF. (1) The brief asked to extend shared/contract.ts with 'the per-skill capability the renderer needs to render its toggle state'; entry 002 already ships it - SkillInfo extends EntityIdentity, which carries capabilities - so nothing was added for it and skills.tsx reads that existing field. (2) docs/plans/001 divergence 3 states project roots arrive with entry 005, and mutations.ts indeed knew only user and desktop. 003 cannot toggle a project skill without one, so createMutations gained an optional extraRoot resolver and resolveIn/rootOf became async; createWorkspace supplies project:<dirName> from its verified-project cache, resolving to <project>/.claude so ADR-0002 confinement is the store boundary itself. The ADR-0001 decision-6 containment check is applied to dynamically resolved roots too. (3) scanSkills had no project-disabled scope, so disabling a project skill would have made it vanish from the list with no way to re-enable it. Added the scope to SkillScope, a matrix row, and a scan of <project>/.claude/skills.disabled. RED-GREEN PROOF, three separate runs, each reverted: swapping appendJournal and act inside mutate() turned 'appends the journal entry before the skill directory moves' red (expected 4 to be less than 0); flipping MATRIX skill/plugin from neither(PLUGIN_OWNED) to ALLOW turned the plugin-refusal test red; misrouting skillPlacement's project case to the user store turned both project-scope tests red. CHECKS RUN: npm run typecheck exits 0 (also npx tsc -b --force, a full rebuild); npm run lint exits 0 (oxlint, silent); npm test 73/73 across 13 files, up from 64/12, every pre-existing test passing unchanged; node .jig/checks/run.mjs reports no findings. test/skill-toggle.test.ts holds 9 cases: user disable lands in skills.disabled and nowhere else with an empty trash, user enable, undo restores the tree byte-for-byte, a project skill toggles both ways inside its own .claude while the user store's hash is unchanged, a write-boundary sweep over the project toggle, the plugin refusal in both directions with no journal entry, the already-enabled refusal, journal-before-move ordering, and refusals for a free-form path, an unknown id and an unknown operation. Both project cases ran (tmpdir has no hyphen); they self-skip on a hyphenated tmpdir like boundary.test.ts. hashTree, exists and recordWrites moved from test/mutation.test.ts into test/helpers.ts so both suites share one write-path probe; mutation.test.ts's 12 cases are otherwise untouched. SCOPE DRIFT beyond the stated surface: CHANGELOG.md, docs/domain.md and docs/testing.md. docs/README.md makes a PR that leaves a doc wrong incomplete; domain.md gained the project-scope skills.disabled convention with a diamond (expected) marker, honestly noting it is unobserved in the wild and written because ADR-0006 chose the scoped equivalent. No docs/plans/ file was written - docs/plans/README.md says a plan is a feature's first commit, not a document written afterwards. One PostToolUse jig firing was a false positive, quoting the raw-path-across-the-seam guard at test/skill-toggle.test.ts (the test that proves a path IS refused); the real driver reports nothing. Work is on branch foreman/003-enable-disable-skills, created from main; this repo has no git remote.\n2026-08-29 scope drift — touched but unpredicted: CHANGELOG.md, docs/domain.md, docs/testing.md, electron/main/ipc.ts, electron/preload/index.ts\n2026-08-29 lesson recorded: .foreman/notes.jsonl, area electron/main\n2026-08-29 verified (was unverified): the user ran the app and confirmed the Skills toggle. The visual check is settled - no unverified checks remain on this entry.\n2026-08-29 scope drift — touched but unpredicted: .foreman/notes.jsonl, CHANGELOG.md, ROADMAP.jsonl, docs/domain.md, docs/testing.md, electron/main/ipc.ts, electron/preload/index.ts\n2026-08-29 2026-08-29 branch foreman/003-enable-disable-skills was squashed into main as 9e3f64c and deleted; the pre-squash sha 50ea668 no longer exists.","model":"opus"}
{"id":"004","title":"Enable and disable plugins per scope","why":"A plugin's enabled state lives in enabledPlugins in a settings layer, and today kondo only reports which layers list it. Toggling by hand means editing JSON in the right file.","what":"Edit enabledPlugins in the chosen settings layer through the journal, preserving formatting and unrelated keys. The plugins view gains a per-scope toggle showing which layer wins. A layer that does not exist yet is created only on explicit confirmation.","status":"done","source":"user","depends_on":["001","002"],"planned_touches":["electron/main/workspace/user-store.ts","src/features/plugins/","shared/contract.ts","test/"],"observed_touches":["CHANGELOG.md","docs/domain.md","docs/testing.md","electron/main/ipc.ts","electron/main/workspace/kinds.ts","electron/main/workspace/mutations.ts","electron/main/workspace/user-store.ts","electron/main/workspace/workspace.ts","electron/preload/index.ts","shared/contract.ts","src/features/plugins/plugins.tsx","test/plugin-toggle.test.ts",".foreman/notes.jsonl","ROADMAP.jsonl",".claude/rules/jetbrains-mcp.md"],"commits":["0e14177","75e1096","9d66b5e"],"created_at":"2026-08-28","updated_at":"2026-08-29","notes":"2026-08-29 unverified: run npm run dev, open the Plugins tab and look at the new Enabled in column. Each plugin row now carries one button per settings layer reading e.g. \"global on\", \"project off\", \"local —\", and the layer that wins is ringed in the accent colour with a ★. Check that clicking a button flips that one layer without a manual refresh, that a plugin no layer mentions still shows the \"nowhere\" pill, and that clicking a layer whose file does not exist raises the card asking to create it with a Create it / Cancel pair rather than writing anything. Every one of these is proven headlessly in test/plugin-toggle.test.ts (16 cases); what no command can settle is how a row reads when a machine has many verified projects, since project and local buttons then repeat with only their tooltip to tell them apart.\n2026-08-29 2026-08-29 DISCREPANCY WITH THE BRIEF. The handoff listed enabledPlugins as \"Unresolved in the entry’s own description (not found in any touched file) - an invented API or an un-caught rename\". It is neither: user-store.ts already read it in pluginEnabledIn, ADR-0006 names it, and ~/.claude/settings.json on this machine holds it as an object keyed by <plugin>@<marketplace> with boolean values, both true and an explicit false observed. Nothing was invented to make the prompt true. 2026-08-29 SHIPPED. The toggle is one journaled write step whose content is the settings file spliced, never reserialized: user-store.ts gained a ~120-line JSON span scanner (skipWs / scanString / scanValue / readObject / insertMember) and editEnabledPlugins(source, key, enabled), which replaces exactly the span holding the plugin’s value, or inserts a new member led by the whitespace the file already puts before its first member - so the file’s own indentation is copied rather than imposed. Everything outside that span is byte-identical, which the tests assert as string equality against the original text, not as a parsed comparison. Array-form enabledPlugins and a non-object root return null and the caller refuses rather than reformatting (ADR-0005). SettingsLayer now carries source (the bytes as read), store and relative, so one read feeds both the parse and the splice and no step ever carries an absolute path. pluginStateIn returns boolean | null - a layer that says nothing cannot win over one that does - and scanPlugins builds PluginScopeState per layer in precedence order (local > project > user, domain.md) plus winningLayerId. kinds.ts gained pluginTogglePlan: the entity-level enable/disable seats stay empty for plugin on purpose, because a plugin’s state is a key in a layer rather than a property of the plugin, so the seat has no target to name; that is documented on the kind. The matrix from 002 is still the gate and it is keyed on the layer the write lands in, not on the install scope. workspace.pluginToggle builds one KindContext for both the entity read and the plan, so the plan sees exactly the bytes the entity was built from. 2026-08-29 CONFIRMATION GATE. A layer whose file does not exist refuses with the new needs-confirmation ScanErrorCode and writes nothing; the renderer raises a card naming the path and only a Create it click repeats the call with createLayer true, producing a file holding just that one key. Undo removes it again (proven). 2026-08-29 BUG FOUND AND FIXED OUTSIDE THE STATED SURFACE: electron/main/workspace/mutations.ts. trashPath used the store name verbatim as a path segment, and a project store is named project:<dirName>. On Windows no path segment may hold a colon, so mkdir failed with ENOENT and three tests went red - every project-scope settings edit was unwritable and its undo unrestorable. Entry 003 never hit it because a skill toggle is a move step, which displaces nothing into the trash. Fixed at the root, in trashPath itself, so the write and its undo cannot disagree. This is real work beyond the entry’s file surface and is flagged as such. 2026-08-29 RED-GREEN PROOF, four probes, each reverted: swapping LAYER_RANK local and user turned both precedence tests red; swapping appendJournal and act inside mutate() turned the journal-ordering test red; inverting the createLayer gate turned five tests red including the two confirmation cases; replacing insertMember’s copied lead with a hard-coded newline-plus-two-spaces turned the two insertion tests red. Before the trashPath fix the three project-scope failures were red for the reason above and green after. 2026-08-29 CHECKS RUN: npm run typecheck exits 0 (also npx tsc -b --force, a full rebuild); npm run lint exits 0 (oxlint, silent); npm test 89/89 across 14 files, up from 73/13, every pre-existing test passing unchanged; node .jig/checks/run.mjs reports no findings. test/plugin-toggle.test.ts holds 16 cases: the value flip proven as exact string equality against the original file, the insertion in the file’s own four-space indentation, unrelated keys and their ordering intact, enabledPlugins added to a layer that lacks the key, the three-layer precedence order, the winner moving from user to project to local as each is written, no winner when no layer speaks, the confirmation refusal leaving the file absent and the journal empty, the confirmed creation, undo restoring byte-for-byte, undo removing a created layer, journal-before-write ordering, a write-boundary sweep over the project layer, the already-in-that-state refusal, five bad-input refusals, and a non-JSON settings file refused rather than rewritten. Project-scope cases self-skip on a hyphenated tmpdir, as in skill-toggle.test.ts; they ran here. 2026-08-29 SCOPE DRIFT beyond the stated surface: electron/main/workspace/kinds.ts and workspace.ts (the plan and the API method), electron/main/ipc.ts and electron/preload/index.ts (a new channel cannot ship without them - the same drift entry 003 had), electron/main/workspace/mutations.ts (the bug above), and CHANGELOG.md, docs/domain.md, docs/testing.md, because docs/README.md treats a PR that leaves a doc wrong as incomplete. domain.md gained the observed enabledPlugins shape marked ✅ and a note that layers of different projects share a precedence rank, since Claude resolves settings per session and across projects there is no ordering to have. No docs/plans/ file was written: docs/plans/README.md says a plan is a feature’s first commit, not a document written afterwards. 2026-08-29 One PostToolUse jig firing was a false positive, quoting the ids-across-the-seam guard at test/plugin-toggle.test.ts - the test that proves a raw path IS refused; the real driver reports no findings. 2026-08-29 The checkpoint protocol’s task 1/3 commit was folded back with git reset before the final commit, so the whole entry lands as one commit and observed_touches records every file rather than only the last third. Work is on branch foreman/004-plugin-toggle-per-scope, created from main; this repo has no git remote.\n2026-08-29 scope drift — touched but unpredicted: CHANGELOG.md, docs/domain.md, docs/testing.md, electron/main/ipc.ts, electron/main/workspace/kinds.ts, electron/main/workspace/mutations.ts, electron/main/workspace/workspace.ts, electron/preload/index.ts\n2026-08-29 lesson recorded: .foreman/notes.jsonl, area electron/main\n2026-08-29 scope drift — touched but unpredicted: .foreman/notes.jsonl, CHANGELOG.md, ROADMAP.jsonl, docs/domain.md, docs/testing.md, electron/main/ipc.ts, electron/main/workspace/kinds.ts, electron/main/workspace/mutations.ts, electron/main/workspace/workspace.ts, electron/preload/index.ts\n2026-08-29 2026-08-29 The unverified UI check came back with a finding, fixed in 75e1096. The chips were ordered by precedence rank across all layers, so five verified projects produced five identical local chips then five identical project chips, distinguishable only by tooltip. PluginScopeState now carries project (the owning folder name, null for the user layer), set from path.basename(project.absPath) in readSettingsLayers, and plugins.tsx groups the chips under that name with the user layer first and project before local inside each group. The two-per-project shape is correct and unchanged: every verified project contributes a settings.json (project) and a settings.local.json (local) layer. One new case in test/plugin-toggle.test.ts asserts the owner is carried; 90/90 tests, typecheck, lint and jig all green. Still awaiting the users eyes on the regrouped column.\n2026-08-29 2026-08-29 verified (was unverified): the user ran the app, exercised the toggle across the global and project layers, and confirmed the regrouped column. Six real toggles are in the journal, four of them cancelling out; the real ~/.claude/settings.json came through with all 23 top-level keys, its two-space formatting and its original enabledPlugins value, which is the formatting-preservation invariant proven outside a fixture. No unverified checks remain on this entry.\n2026-08-29 2026-08-29 branch foreman/004-plugin-toggle-per-scope was squashed into main as 9d66b5e and deleted; the pre-squash shas 0e14177 and 75e1096 no longer exist. All three checks re-run on main after the squash: 90/90 tests, typecheck 0, lint 0.\n2026-08-29 scope drift — touched but unpredicted: .claude/rules/jetbrains-mcp.md, .foreman/notes.jsonl, CHANGELOG.md, ROADMAP.jsonl, docs/domain.md, docs/testing.md, electron/main/ipc.ts, electron/main/workspace/kinds.ts, electron/main/workspace/mutations.ts, electron/main/workspace/workspace.ts, electron/preload/index.ts","model":"opus","effort":"high"}
{"id":"005","title":"Move a skill between scopes and projects","why":"Skills accumulate in the wrong scope and moving one by hand means copying a directory and hoping nothing referenced the old path. This is the feature the tool is named for.","what":"Copy, verify, then trash the source, all journaled as one reversible operation. Handles user to project, project to user, and project to project. Refuses a move whose destination already holds that skill name rather than merging, and refuses plugin-owned sources outright.","status":"done","source":"user","depends_on":["001","003"],"planned_touches":["electron/main/workspace/","src/features/skills/","test/"],"observed_touches":["CHANGELOG.md","docs/plans/001-mutation-journal-and-trash.md","docs/testing.md","electron/main/ipc.ts","electron/main/workspace/capabilities.ts","electron/main/workspace/kinds.ts","electron/main/workspace/mutations.ts","electron/main/workspace/workspace.ts","electron/preload/index.ts","shared/contract.ts","src/features/skills/skills.tsx","test/plugin-toggle.test.ts","test/skill-move.test.ts","test/skill-toggle.test.ts",".foreman/notes.jsonl","ROADMAP.jsonl","docs/domain.md","electron/main/workspace/user-store.ts","test/kinds.test.ts","test/user-store.test.ts"],"commits":["6e0d08f"],"created_at":"2026-08-28","updated_at":"2026-08-29","notes":"2026-08-29 unverified: run npm run dev, open the Skills tab, and look at the new Move to column between Location and the toggle button. Check that the select lists global (~/.claude) plus one entry per project whose path was reconstructed, that picking one moves the row to its new scope after a single click with no manual refresh, that a plugin-shipped skill shows the select greyed out with the matrix reason as its tooltip (Plugin-shipped skills follow their plugin - toggle the plugin instead), and that picking the scope the skill is already in renders the red refusal card above the table reading is already in that scope. Every one of these is proven headlessly in test/skill-move.test.ts; what no command can settle is whether a dropdown is the right affordance here and whether the project paths are readable at that width - they are full absolute paths, which can be long.\n2026-08-29 2026-08-29 Shipped the move as one journaled operation whose step order IS the invariant. mutations.ts gained a cross-store `copy` PlannedStep — {store, from, toStore, to} — and skillMovePlan emits exactly two steps, copy then trash, so `undo` reverses the pair or neither. Verification is a real proof, not an existence check: digestTree() sha256s a tree's sorted relative names plus every file's bytes, and verifyCopy() compares source to destination before runSteps is allowed to reach the trash step. A copy that does not verify is relocated into that entry's own trash (never unlinked) and the step throws a Refused, so the source is never released. mutate()'s act-phase catch now honours Refused so that reason reaches the seam instead of a generic read-failed. undo() reverses a copy by displacing the destination into the undo's own trash, guarded by exists() so a failed copy does not break the undo; dropCreated is keyed on createdIn(step) because a copy's created directories live under toStore, not store. relocate()'s EXDEV fallback was upgraded from `exists(to)` to the same verifyCopy — that path is not hypothetical here, a D: project store and a C: <kondo-data> hit it on every project-skill trash. SEAM: CapabilityOperation gained a third member, `move`, so the matrix answers it separately (a `user` skill denies enable yet is perfectly movable — reusing the toggle rows would have been wrong reasoning). ToggleOperation = enable|disable was extracted so skillToggle/pluginToggle cannot be handed `move` at the type level. New method skillMove(skillId, destinationId) + channel kondo:skill-move; destinationId is `user` or a project:code:<dirName> id from a previous scan, never a path (ADR-0008). MATRIX: skill rows user/user-disabled/project/project-disabled get move:ALLOW, skill/plugin becomes none(PLUGIN_OWNED) so the plugin refusal carries its own reason for all three operations, and every other kind denies move with ONLY_SKILLS_MOVE. DECISIONS BEYOND THE BRIEF, both stated rather than assumed: (1) the collision check counts BOTH of the destination scope's directories — a name sitting in its skills.disabled is the same name arriving twice — implemented by reusing skillPlacement over the single scan the workspace already holds, with planSteps' own destination-exists check as the fs-level backstop; (2) the move preserves enabled state, so a benched skill lands in the destination's skills.disabled and stays benched (ADR-0006 — the store is the state). RED-GREEN PROOF, three separate sabotages, each reverted and re-run green: stubbing verifyCopy to always pass turned the failed-verification test red (expected data toBeNull); swapping the plan's copy and trash steps turned 7 of 11 red with read-failed on every direction; flipping MATRIX skill/plugin move to ALLOW turned the plugin refusal red (the message fell through to skillPlacement's own refusal, confirming the matrix is the gate rather than a second guard). CHECKS RUN: npm run typecheck exits 0; npm run lint exits 0 (oxlint, silent); npm test 101/101 across 15 files, up from 90/14, every pre-existing test passing unchanged; npm run guards reports No findings. test/skill-move.test.ts holds 11 cases: all three directions, the enabled-state-preserving move, the name collision in skills and again in skills.disabled, the plugin-owned refusal, already-in-that-scope plus unknown-destination plus a free-form path as destination, journal-before-destination ordering, the unverified copy leaving the source byte-for-byte and no half-copy behind, and undo restoring the source AND removing the copy with both trees hashed. Two fixture projects were added so project-to-project is a real direction; those cases self-skip on a hyphenated tmpdir, and they ran here. SCOPE DRIFT beyond the stated surface (electron/main/workspace/, src/features/skills/, test/): shared/contract.ts, electron/main/ipc.ts and electron/preload/index.ts — a mutation cannot reach the renderer without crossing the seam, and entry 003 took the same three. Plus CHANGELOG.md and docs/testing.md, which docs/README.md makes mandatory, and one line of docs/plans/001 whose divergence 3 still said project roots arrive with 005 when 003 had already added them. test/skill-toggle.test.ts and test/plugin-toggle.test.ts each changed one import and one cast, forced by the ToggleOperation narrowing. DISCREPANCY WITH THE PROMPT: its prior_work cited docs/plans/001 divergence 3 as pending for this entry; it was already delivered by 003, so this entry inherited project stores rather than adding them, and that line is now corrected. KNOWN LIMIT, pre-existing and not introduced here: because the journal is appended before any step runs, a move whose copy fails to verify still leaves a journal entry describing a move that did not happen, and undoing that entry throws because the trash step never ran. Every step failure in mutations.ts has behaved this way since 001 (the comment at the act-phase catch says so); recording it rather than widening this entry to redesign the journal. No docs/plans/ file was written — docs/plans/README.md says a plan is a feature's first commit, not a document written afterwards. Two PostToolUse jig firings were false positives, quoting the renderer-purity guard at electron/main/workspace/ files and the ids-across-the-seam guard at test/skill-move.test.ts (the test that proves a path IS refused); npm run guards reports nothing. Work is on branch foreman/005-move-skill-between-scopes, created from main; this repo has no git remote.\n2026-08-29 scope drift — touched but unpredicted: CHANGELOG.md, docs/plans/001-mutation-journal-and-trash.md, docs/testing.md, electron/main/ipc.ts, electron/preload/index.ts, shared/contract.ts\n2026-08-29 lesson recorded: .foreman/notes.jsonl, area electron/main\n2026-08-29 2026-08-29 branch foreman/005-move-skill-between-scopes was squashed into main as 6e0d08f and deleted; the pre-squash shas a455c4e and 03ec66b no longer exist. Accepted by the user, who ran no visual check - the unverified Move to picker note above stands as unconfirmed by eye.\n2026-08-29 scope drift — touched but unpredicted: .foreman/notes.jsonl, CHANGELOG.md, ROADMAP.jsonl, docs/domain.md, docs/plans/001-mutation-journal-and-trash.md, docs/testing.md, electron/main/ipc.ts, electron/preload/index.ts, shared/contract.ts\n2026-08-29 2026-08-29 verified (was unverified): the app was built and launched against a synthetic fixture store via KONDO_STORE_ROOT / KONDO_DESKTOP_STORE_ROOT / KONDO_DATA_ROOT, never a real ~/.claude, and driven over Electrons remote debugging port with a throwaway CDP script (Node 22s global WebSocket, no Playwright added). Confirmed by eye and on disk: the Move to column renders between Location and the toggle; the picker lists global (~/.claude) plus one entry per reconstructed project; picking one moved commit-writer from the user store into the apiserver project in a single click with no manual refresh, the row re-reading to the project scope pill and the new Location; on disk the destination held the skill, the source directory was gone, the source bytes sat under kondo-data/trash/<journal-id>/user/skills/commit-writer/SKILL.md, and journal.jsonl held exactly one entry whose steps were copy then trash in that order. The already-in-that-scope refusal and the name-collision refusal both rendered as the red card above the table with neither side modified. Entry 011 confirmed too: the fixtures plugin-shipped skill never appeared in the list. TWO COSMETIC FINDINGS, neither blocking, both logged separately: the sidebar footer in src/app/app.tsx line 46 still reads read-only preview - v0.1 although three mutations now ship; and the collision refusal names the colliding skills own directory rather than the destination scope, so it reads ...skills/db-migrate already holds a skill named db-migrate.","model":"opus","effort":"high"}
{"id":"006","title":"Add the tidy sweep with a dry-run preview","why":"A store with 8,921 project directories accumulates stale sessions, empty transcripts and dead caches that nobody will clear by hand. Reclaiming that space is why staleness is measured at all.","what":"Bulk-archive stale sessions, sweep empty transcripts and orphaned sidecars, and clear reclaimable cache directories, each through the trash. Every sweep shows a dry-run preview naming counts and bytes before anything moves, and the whole sweep is one journal entry so undo restores it together.","status":"done","source":"user","depends_on":["001"],"planned_touches":["electron/main/workspace/","src/features/","shared/contract.ts","test/"],"observed_touches":["electron/main/ipc.ts","electron/main/workspace/capabilities.ts","electron/main/workspace/sessions.ts","electron/main/workspace/tidy.ts","electron/main/workspace/workspace.ts","electron/preload/index.ts","shared/contract.ts","src/app/app.tsx","src/features/tidy/tidy.tsx","test/sessions.test.ts","test/tidy.test.ts"],"commits":[],"created_at":"2026-08-28","updated_at":"2026-08-29","notes":"2026-08-29 Shipped the tidy sweep end to end. electron/main/workspace/tidy.ts scans four categories off the cached tier-1 inventory (stale sessions carrying their sidecar, empty transcripts, orphaned sidecars, and a fixed allowlist of reclaimable cache directories: cache, paste-cache, debug, downloads, shell-snapshots, telemetry -- backups/ and file-history/ deliberately excluded because they back checkpoint/rewind), projects them into TidyPreview, and builds ONE MutationPlan of trash steps. workspace.tidyPreview/tidySweep wire it; a successful sweep or undo now drops the cached inventory so the next read rescans. shared/contract.ts gains tidyCategories, TidyCategoryPreview, TidyPreview, the two API methods and their channels, plus a 'store' EntityKind (with its capabilities.ts row) so the journal entry names what it acted on rather than borrowing 'session'. SessionRecord.hasSidecar became sidecar: string|null so the sweep displaces the sidecar's real directory name rather than a lowercased guess. Renderer: src/features/tidy/tidy.tsx, a per-category checkbox table plus a confirm bar, re-reading the preview after the sweep. Checks all run here, none handed back: npm run typecheck exit 0, npm run lint exit 0, npm run guards no findings, npm test 16 files / 112 tests pass (test/tidy.test.ts adds 10, covering preview-moves-nothing, moved-set-equals-previewed-set, category filtering, bad-request refusals, one journal entry, undo restoring byte-for-byte, journal-before-store ordering, the post-sweep fresh read, and the empty-store no-op). Visual check done with the run-kondo skill against a seeded D:/Temp/kondofix fixture: the preview read 5 items / 942 KB, the confirm bar read 'Move 5 items (942 KB) into kondo's trash?', pressing Sweep wrote exactly one journal.jsonl entry (op trash, kind store, entityId store:user, 6 trash steps), all six paths landed under kondo-data/trash/<id>/user/, backups/ and plugins/cache survived untouched, and the Dashboard re-read from 4 sessions / 2 stale / 846 B to 2 / 0 stale / 646 B. Two discrepancies against the prompt and the repo's rules. (1) .claude/rules/jetbrains-mcp.md assumes a WebStorm MCP server, but no mcp__webstorm__* tool exists in this session -- native tools were used throughout. (2) The jig PostToolUse hook blocked both new-file writes with the renderer-reaches-past-the-bridge message, for electron/main/workspace/tidy.ts and test/tidy.test.ts, neither of which is under src/; running node .jig/checks/run.mjs on those exact paths and on the whole repo reports no findings, so the hook's path matching is wider than the check's own src/**/*.ts glob. Entry 001's recorded lesson (journal.jsonl append-only, undoneBy derived from undoOf) verified true and is unchanged. Flagged rather than written silently: no docs/plans/006-*.md, because CONTRIBUTING.md and AGENTS.md want a plan file per feature but docs/ is outside this entry's declared file surface. No undo button in the Tidy view either -- entry 007 owns the undo and trash UI, and every earlier mutation is in the same position.\n2026-08-29 scope drift — touched but unpredicted: electron/main/ipc.ts, electron/preload/index.ts, src/app/app.tsx\n2026-08-29 lesson recorded: .foreman/notes.jsonl, area electron/main","model":"opus"}
{"id":"007","title":"Surface undo and the trash in the UI","why":"ADR-0001 makes undo a first-class feature, so it needs a first-class surface. Trashed transcripts persist until emptied, and SECURITY.md requires the size to be visible.","what":"A view listing journal entries newest first, each with what it did and an undo action, plus trash size and an explicit empty-trash action that is never bundled into another operation. Emptying is the one destructive act kondo has and must read like it.","status":"done","source":"user","depends_on":["001"],"planned_touches":["src/features/","shared/contract.ts","electron/main/workspace/"],"observed_touches":["CHANGELOG.md","docs/domain.md","docs/plans/007-undo-and-trash-ui.md","docs/plans/README.md","electron/main/workspace/mutations.ts","test/journal.test.ts",".foreman/notes.jsonl","ROADMAP.jsonl"],"commits":["e78dcbf","b15413d"],"created_at":"2026-08-28","updated_at":"2026-08-29","notes":"2026-08-29 Shipped the Journal tab (src/features/journal/journal.tsx, nav entry in src/app/app.tsx): entries newest first with summary, entity id, kind/op pills, step count and a per-entry Undo through the existing undo(journalId); trash size, restore-point count and root path above it; empty-trash behind a confirmation naming what is lost, Cancel first and focused. Added trashEmpty to shared/contract.ts (channel kondo:trash-empty), mutations.emptyTrash() and workspace.trashEmpty(); trashSize body extracted to a shared readTrash() so emptyTrash measures before and after and reports the difference. Not journaled by design. Also fixed at source: a stranded undo returned a raw ENOENT rename error, seen on screen during the fixture run; mutations.undo now branches on isEnoent and names the cause. Checks run: npm run typecheck exit 0, npm run lint exit 0, npm test 120/120 across 17 files including the 8 new test/journal.test.ts cases and the 12 existing ADR-0001 invariants; npm run guards no findings. Visual checks all RUN, not deferred, via the run-kondo skill against a synthetic fixture at X:/Temp/kondofix (torn down; no real store touched): newest-first ordering, 113 B / 1 restore point sizing, the confirmation reading Permanently delete 113 B from 2 restore points with document.activeElement == Keep the trash, undo re-reading both sources and marking the original undone, and the stranded-undo refusal now rendering as a sentence. FINDINGS: (1) journalList, journalUndo and trashSize already existed on the seam from entry 001 - the prompt implied all four needed adding; only trashEmpty was missing. (2) Files touched beyond the stated expected surface, all forced: electron/main/ipc.ts and electron/preload/index.ts (channels satisfies Record<keyof KondoApi,string>, so a new API method cannot typecheck without both), src/app/app.tsx (otherwise the view is unreachable), test/journal.test.ts (tests were required), and CHANGELOG.md + docs/adr/0001 + docs/domain.md + docs/plans/ (AGENTS.md docs-move-with-code plus the two jig doc-pairing guards). (3) jig guard renderer-reaches-past-the-bridge PostToolUse lane false-positives on test/*.ts for node: imports though its own paths are src/**/*.ts; its checks lane scopes correctly and npm run guards is clean with every existing test file present. Not fixed - jig-owned and out of scope.\n2026-08-29 scope drift — predicted but untouched: src/features/, shared/contract.ts; touched but unpredicted: CHANGELOG.md, docs/domain.md, docs/plans/007-undo-and-trash-ui.md, docs/plans/README.md, test/journal.test.ts\n2026-08-29 lesson recorded: .foreman/notes.jsonl, area docs/plans\n2026-08-29 scope drift — predicted but untouched: src/features/, shared/contract.ts; touched but unpredicted: .foreman/notes.jsonl, CHANGELOG.md, ROADMAP.jsonl, docs/domain.md, docs/plans/007-undo-and-trash-ui.md, docs/plans/README.md, test/journal.test.ts","model":"opus","effort":"high"}
{"id":"008","title":"Register jig session guards in .claude/settings.json","why":"The four jig guards are armed in .jig/config.json but nothing invokes the runner on PostToolUse, so they fire only in CI and npm run guards, never live while a session edits.","what":"Add a PostToolUse hook on Edit and Write that calls jig hooks/runner.js. The obstacle is the path: the runner sits at a version-pinned plugin cache path, so committing it breaks other machines and every plugin upgrade. Solve that first with a repo-local shim, a settings.local.json entry, or a resolver, then wire it. Confirm the guards deny live afterwards.","status":"done","source":"claude-suggested","depends_on":[],"planned_touches":[".claude/settings.json",".jig/"],"observed_touches":[".jig/hooks/session-guards.cjs",".claude/settings.json"],"commits":[],"created_at":"2026-08-28","updated_at":"2026-08-29","notes":"2026-08-29 unverified: restart Claude Code, edit any file, and confirm a jig verdict comes from the repo hook — this session loaded its hooks before .claude/settings.json existed, so only a fresh session can show that registration firing.\n2026-08-29 Wired through .jig/hooks/session-guards.cjs (committed in c8e4fa1), which .claude/settings.json invokes as `node .jig/hooks/session-guards.cjs PostToolUse` on matcher Edit|Write. The shim resolves jig at call time - marketplace checkout first, newest cached version second, silent exit where jig is absent - and requires runner.js rather than spawning it, so no version-pinned path is written down anywhere. It is .cjs because package.json sets type:module. Ran: npm run guards exits 0 with 0 findings and 0 broken; run.mjs --selftest shows all six checks catching their own violation. Ran: the exact hook command prints a verdict - pass on a clean payload, top-level decision:block carrying the guard's reason on a violating one. Ran: CLAUDE_CONFIG_DIR pointed at a synthetic cache holding 2.0.0 and 10.1.0 picks 10.1.0 (numeric, not lexical); an empty config dir exits 0 in silence. Premise correction 1: the guards were ALREADY firing live before this task. jig@slag is enabled in ~/.claude/settings.json and the plugin ships hooks/hooks.json registering PostToolUse[Edit|Write] on the same runner; .jig/ledger.jsonl holds 47 pre-existing deny rows from claude-session edits. What was missing was a repo-owned path, not the hook itself. Where both are wired the guards run twice, which inflates the fired count /jig:review reports and changes no decision - the only latch jig reads off the ledger is a standing false positive. Premise correction 2: .claude/settings.json is gitignored by .gitignore:20 (.claude/*, deliberate per the comment above it), so this registration is per-machine - the shape the entry sanctioned as a settings.local.json entry, but a clone gets the shim with nothing invoking it. Un-ignoring it would edit .gitignore, outside this task's declared surface, so it is left as the user's call. Finding, out of scope and not fixed: jig-lib.js evalEdit never reads a detector's params.paths, so renderer-reaches-past-the-bridge denies legitimate node: imports under electron/main/ and test/. Visible in the pre-existing ledger rows for electron/main/workspace/tidy.ts, test/tidy.test.ts and .claude/skills/run-kondo/drive.mjs.\n2026-08-29 lesson recorded: .foreman/notes.jsonl, area .jig/hooks\n2026-08-29 Live check settled, so the unverified line above is closed. After the session restart, one Write of test/hook-probe.ts containing os.homedir() came back blocked twice: once from node ${CLAUDE_PLUGIN_ROOT}/hooks/runner.js PostToolUse, which is jig's own plugin hook, and once from node .jig/hooks/session-guards.cjs PostToolUse, which is this entry's registration in .claude/settings.json. .jig/ledger.jsonl grew by exactly 8 rows for that single Write, 4 per hook, confirming both that the repo registration fires and the double-run predicted at close. The probe file was removed and the tree is clean. The deny text named src/ for a file under test/, which is the path-scope defect tracked as entry 017.","model":"opus","effort":"xhigh"}
{"id":"009","title":"Trash path cannot hold a project store name on Windows","why":"electron/main/workspace/mutations.ts trashPath joined the store name verbatim into <kondo-data>/trash/<journal-id>/, and a project store is named project:<dirName>. No Windows path segment may hold a colon, so mkdir failed with ENOENT: every project-scope write or trash step was unwritable and its undo unrestorable. Entry 003 never hit it because a skill toggle is a move step, which displaces nothing into the trash; entry 004 hit it on the first project-scope settings edit.","what":"trashPath now replaces the colon with a dash before splitting the displaced path into segments. Both the write and its undo route through that one function, so they cannot disagree on where the displaced bytes went. The journal still records the real store name. Proven by three cases in test/plugin-toggle.test.ts that were red before the fix and green after.","status":"done","source":"claude-suggested","depends_on":[],"planned_touches":["electron/main/workspace/mutations.ts"],"observed_touches":[".foreman/notes.jsonl","CHANGELOG.md","ROADMAP.jsonl","docs/domain.md","docs/testing.md","electron/main/ipc.ts","electron/main/workspace/kinds.ts","electron/main/workspace/mutations.ts","electron/main/workspace/user-store.ts","electron/main/workspace/workspace.ts","electron/preload/index.ts","shared/contract.ts","src/features/plugins/plugins.tsx","test/plugin-toggle.test.ts",".claude/rules/jetbrains-mcp.md"],"commits":["0e14177","9d66b5e"],"created_at":"2026-08-29","updated_at":"2026-08-29","notes":"2026-08-29 2026-08-29 Fixed inside entry 004 rather than on its own commit, so 0e14177 carries both. Recorded separately because the file is outside 004 planned surface and the bug is a distinct concern.\n2026-08-29 scope drift — touched but unpredicted: .foreman/notes.jsonl, CHANGELOG.md, ROADMAP.jsonl, docs/domain.md, docs/testing.md, electron/main/ipc.ts, electron/main/workspace/kinds.ts, electron/main/workspace/user-store.ts, electron/main/workspace/workspace.ts, electron/preload/index.ts, shared/contract.ts, src/features/plugins/plugins.tsx, test/plugin-toggle.test.ts\n2026-08-29 2026-08-29 landed inside the 004 squash as 9d66b5e; the pre-squash sha 0e14177 no longer exists.\n2026-08-29 scope drift — touched but unpredicted: .claude/rules/jetbrains-mcp.md, .foreman/notes.jsonl, CHANGELOG.md, ROADMAP.jsonl, docs/domain.md, docs/testing.md, electron/main/ipc.ts, electron/main/workspace/kinds.ts, electron/main/workspace/user-store.ts, electron/main/workspace/workspace.ts, electron/preload/index.ts, shared/contract.ts, src/features/plugins/plugins.tsx, test/plugin-toggle.test.ts","model":"opus","effort":"high"}
{"id":"010","title":"A failed mutation leaves a journal entry whose undo throws","why":"mutate() appends the journal entry before any step runs (ADR-0001 decision 5 - an unjournaled write is an un-undoable one), so a step that fails mid-operation leaves an entry on disk describing work that did not happen. journalList shows it as a completed operation, and journalUndo on it throws: undo of a trash step unconditionally relocates from trashPath(original.id, ...), which was never written. Observed while adding the verified copy step in 005 - a move whose copy fails verification is exactly this shape. Entry 007 puts undo in front of users, which turns a throw into something they will hit.","what":"Decide and implement what the journal records about a partially-run operation. Options seen from 005: record which steps actually ran (a second append is not allowed - the file is append-only per the 001 lesson, so this likely means the entry carries per-step outcome only via a following compensating entry), or make undo tolerant per step - skip a trash restore whose trash directory is absent while the source is present, and skip a move whose destination is absent. mutations.ts act() already guards the copy and write reversals with exists(); the trash reversal is the one that does not. Whatever is chosen, journalList must stop presenting a failed operation as a done one.","status":"done","source":"claude-suggested","depends_on":["001"],"planned_touches":["electron/main/workspace/mutations.ts","test/mutation.test.ts"],"observed_touches":["electron/main/workspace/mutations.ts","shared/contract.ts","src/features/journal/journal.tsx","test/mutation.test.ts","ROADMAP.jsonl","docs/adr/0001-mutations-are-reversible.md","docs/domain.md"],"commits":["3e2dcae"],"created_at":"2026-08-29","updated_at":"2026-09-01","notes":"2026-09-01 Confirmed the bug before fixing: a 2-step plan whose second step throws left an entry whose undo returned read-failed with the emptied-trash message, blaming the wrong cause. Test probe run against unmodified mutations.ts. Chose option (b) plus a marker. undo() now guards the move and trash reversals with exists(), matching the guards already on copy and write: a move whose destination is absent while the source is present never ran, and a trash restore whose trash directory is absent while the source is present never ran. Both are skipped. When the source is ALSO absent the relocate still runs and still ENOENTs, so the emptied-trash message survives; a new test pins that. journalList honesty: write() catches a step failure and appends one more line, a marker record carrying failedOf plus an empty steps array. list() derives failed:true for the id it names and filters the marker itself out of the list. journal.jsonl stays strictly append-only (the 001 lesson holds). Scope beyond planned_touches, approved by the user before writing: JournalEntryInfo had no field able to express a partly-run operation, so shared/contract.ts gained failed:boolean and src/features/journal/journal.tsx renders a failed pill beside the existing undone one. Verified: npm test 123/123 pass across 17 files, npm run typecheck clean, npm run lint (oxlint) exit 0. Three new cases in test/mutation.test.ts cover a failing later step, a failing trash step, and the emptied-trash path. UI verified by hand with the run-kondo skill, not left unverified: launched the built app at the X:/Temp/kondofix fixture, disabled a project skill, appended a marker line for that entry, reopened Journal. The failed pill renders; the marker line is not listed; Undo stays enabled on a failed entry and completed, leaving the row showing undone plus failed. Fixture and screenshots removed. NOT COMMITTED. safe-commit begin returned dirty:true (.claude/rules/jetbrains-mcp.md was already modified), so per the handoff no commit and no staging was made. All five changed files are left in the working tree. Follow-up worth an entry: the write reversal has the same gap one layer down. If a write step failed after displacing the old bytes but before writing, undo relocates the target into the undo trash and then cp from an absent path, leaving the store short. Guarding it would trade the emptied-trash message for silence on write-only entries, so it needs its own decision rather than a quiet guard here.\n2026-09-01 lesson not recorded (no_observed_paths): electron/main/workspace/mutations.ts marks a partly-run operation with a following marker record carrying failedOf, which list() derives failed from and filters out, and undo() skips any move or trash reversal whose effect is absent while its source is still in place.\n2026-09-01 scope drift — touched but unpredicted: shared/contract.ts, src/features/journal/journal.tsx\n2026-09-01 scope drift — touched but unpredicted: ROADMAP.jsonl, docs/adr/0001-mutations-are-reversible.md, docs/domain.md, shared/contract.ts, src/features/journal/journal.tsx","model":"opus","effort":"high"}
{"id":"011","title":"Exclude plugin-shipped skills from the skills catalogue","why":"They are not the user's to bench or relocate - either one leaves the plugin pointing at a directory that moved - so listing them as permanently greyed-out rows in the Skills tab is clutter that invites a mistake. Raised by the user while reviewing 005.","what":"scanSkills stops walking plugin trees and drops its plugins parameter, so skillsList no longer reads the plugin manifest at all. The plugin skill scope and its capability-matrix row stay as the standing refusal. The installPath confinement check moves from scanSkills into scanPlugins, where the untrusted path is resolved, nulling installAbs at the source.","status":"done","source":"user","depends_on":["005"],"planned_touches":["electron/main/workspace/user-store.ts","electron/main/workspace/kinds.ts","test/","docs/domain.md"],"observed_touches":["CHANGELOG.md","docs/domain.md","electron/main/workspace/kinds.ts","electron/main/workspace/user-store.ts","test/kinds.test.ts","test/plugin-toggle.test.ts","test/skill-move.test.ts","test/skill-toggle.test.ts","test/user-store.test.ts",".foreman/notes.jsonl","ROADMAP.jsonl","docs/plans/001-mutation-journal-and-trash.md","docs/testing.md","electron/main/ipc.ts","electron/main/workspace/capabilities.ts","electron/main/workspace/mutations.ts","electron/main/workspace/workspace.ts","electron/preload/index.ts","shared/contract.ts","src/features/skills/skills.tsx"],"commits":["0f7860e","6e0d08f"],"created_at":"2026-08-29","updated_at":"2026-08-29","notes":"2026-08-29 2026-08-29 Raised by the user against 005: plugin-shipped skills must not appear in the Skills tab, because moving or disabling one would invalidate the plugin. FACTUAL CORRECTION SURFACED FIRST: the danger was already fully blocked - the matrix row skill/plugin is none(PLUGIN_OWNED), which 005 had already extended to cover move, and both mutation paths refused it in the main process rather than the UI. The user was told this and still chose to drop them from the listing, adding that the plugins view can show them later and that needs planning - logged as entry 012. SHIPPED: scanSkills no longer walks plugin trees, and its `plugins: PluginRecord[]` parameter is gone, so kinds.skill.discover no longer calls context.plugins() at all - skillsList stops reading installed_plugins.json and the settings layers it depends on, which is strictly less I/O per ADR-0007. The `plugin` SkillScope and its matrix row are KEPT on purpose: nothing produces that scope today, but the row is the standing refusal 012 will inherit, and kinds.test.ts now asserts both disable and move are denied there. SECOND CHANGE, not asked for but forced by the first: the SECURITY.md confinement check on a plugin installPath lived inside the scanSkills plugin loop, so deleting that loop would have deleted a tested security guard. It moved into scanPlugins where the untrusted path is resolved - an escaping path now nulls installAbs at the source, so no later reader can follow it by forgetting to check, and PluginInfo.installPath still displays the declared path so the manifest's truth is not hidden. This is the razor rule about fixing in the shared function rather than guarding at each caller, and 012 inherits the guard for free. TWO CONSEQUENCES OBSERVED, both fixed rather than papered over: the confinement error now surfaces on every plugin read rather than only on a skills scan, so test/user-store.test.ts line 106 no longer expects an empty collector; and test/plugin-toggle.test.ts used the placeholder installPath 'x' for all three fixture plugins, which is a relative path and therefore correctly flagged as escaping - the fixture now uses real in-store install paths, which is what a real manifest holds. TESTS: the two `refuses a plugin-shipped skill at the capability matrix` cases in skill-toggle and skill-move became `never offers a plugin-shipped skill, and refuses one named directly` - they now assert the id is absent from skillsList, that naming it anyway returns unknown-id with nothing written and no journal entry, and that capabilitiesFor('skill','plugin') still carries the Plugin-shipped reason. Added `never lists a skill that ships inside a plugin` in user-store.test.ts. CHECKS RUN: npm run typecheck exits 0; npm run lint exits 0; npm test 102/102 across 15 files, up from 101/15; npm run guards reports No findings. SCOPE: touched electron/main/workspace/user-store.ts and kinds.ts, four test files, CHANGELOG.md and docs/domain.md, which gained a row for the plugin skills directory saying why it is excluded. No seam change - SkillInfo, the channels and the UI are untouched, because removing rows from a listing needs no new contract.\n2026-08-29 scope drift — touched but unpredicted: CHANGELOG.md\n2026-08-29 lesson recorded: .foreman/notes.jsonl, area test\n2026-08-29 2026-08-29 branch foreman/005-move-skill-between-scopes was squashed into main as 6e0d08f and deleted; the pre-squash sha 0f7860e no longer exists.\n2026-08-29 scope drift — touched but unpredicted: .foreman/notes.jsonl, CHANGELOG.md, ROADMAP.jsonl, docs/plans/001-mutation-journal-and-trash.md, docs/testing.md, electron/main/ipc.ts, electron/main/workspace/capabilities.ts, electron/main/workspace/mutations.ts, electron/main/workspace/workspace.ts, electron/preload/index.ts, shared/contract.ts, src/features/skills/skills.tsx","model":"opus","effort":"high"}
{"id":"012","title":"List a plugin's own skills in the plugins view","why":"005 and 011 took plugin-shipped skills out of the Skills tab because they are not the user's to move or bench. Nothing shows them now, so a user cannot see what skills a plugin contributes. The user asked for this explicitly and said it needs planning first.","what":"Needs a plan before implementation. Surface each plugin's skills read-only under the plugin that owns them, probably as a child listing on the plugins view. The pieces are already in place: PluginRecord.installAbs is the confined install root (scanPlugins nulls it when it escapes the user store), the skill scope plugin and its capability-matrix row none(PLUGIN_OWNED) still refuse all three operations, and scanSkills addFrom already knows how to read a skills directory. Open questions: whether this is a new seam method or a field on PluginInfo, whether reading is lazy per ADR-0007, and what the row shows for a plugin that ships none.","status":"done","source":"user","depends_on":["011"],"planned_touches":["electron/main/workspace/user-store.ts","src/features/plugins/plugins.tsx","shared/contract.ts","test/"],"observed_touches":["docs/adr/0007-stat-first-lazy-scanning.md","docs/domain.md","electron/main/ipc.ts","electron/main/workspace/kinds.ts","electron/main/workspace/user-store.ts","electron/main/workspace/workspace.ts","electron/preload/index.ts","shared/contract.ts","src/features/plugins/plugins.tsx","test/kinds.test.ts","test/plugin-skills.test.ts"],"commits":["b4a147a"],"created_at":"2026-08-29","updated_at":"2026-09-01","notes":"2026-09-01 PLAN, as agreed with the user before implementation. (1) A new seam method, not a field on PluginInfo: pluginSkills(pluginId) -> Scan<SkillInfo[]> on channel kondo:plugin-skills, backed by a new kinds.pluginSkill registry entry keyed on context.parentId - the same shape sessionList(projectId) already uses. (2) Lazy per ADR-0007: listing a skill means opening its SKILL.md, so it happens only for the plugin whose row was opened. test/plugin-skills.test.ts asserts this directly - pluginsList() reads zero SKILL.md, pluginSkills() then reads three. (3) A plugin that ships none expands to 'Ships no skills.' with no error; ENOENT on the skills directory is already swallowed by safeReaddir. BUILT. user-store.ts: lifted scanSkills' addFrom closure to a module-level readSkillDir and added scanPluginSkills, which reads PluginRecord.installAbs and returns [] when scanPlugins nulled it. kinds.ts: pluginSkill entry with ...noPlanYet. workspace.ts + ipc.ts + preload: the seam plumbing. plugins.tsx: each plugin name is now an expander button; the open row gets a colSpan child row rendering PluginSkills, which has no controls in it at all. VERIFIED. npm test 130 passed / 18 files, 7 of them new in test/plugin-skills.test.ts. npm run typecheck clean. npm run lint (oxlint) clean. All four invariants also checked in the running app via the run-kondo skill against a synthetic fixture (never a real store): expanding foreman shows its roadmap and survey skills with descriptions and no buttons; expanding a plugin with no skills/ directory shows 'Ships no skills.'; the Skills tab still lists only the four user/project skills; and the fixture's plugin skills are absent from it. No unverified: lines - every check ran. DISCREPANCY WITH THE BRIEF. The handoff flagged root, installAbs and addFrom as 'Unresolved in the entry's own description ... an invented API or an un-caught rename'. All three were real and unchanged: PluginRecord.installAbs is a field on the record scanPlugins returns, addFrom was the local closure inside scanSkills, and root was its first parameter. Nothing was invented and nothing had been renamed. SCOPE BEYOND planned_touches, flagged to the user and approved before writing. The seam method necessarily added electron/main/ipc.ts, electron/preload/index.ts, kinds.ts and workspace.ts. Then jig's commit-time guards blocked the first commit outright: workspace-adapter-outruns-domain-doc and seam-contract-outruns-its-adr each demand a paired doc edit staged in the same commit, so docs/domain.md's plugin-skills row and a Consequences bullet in docs/adr/0007 went in too. Both edits are factually true of the shipped code. ONE GUARD MISFIRE worth someone's attention: the PostToolUse renderer-reaches-past-the-bridge guard fired on a Write to test/plugin-skills.test.ts, reporting 'src/ is the renderer'. That check's params.paths is [\"src/**/*.ts\", \"src/**/*.tsx\"], and `node .jig/checks/run.mjs` reports No findings over the same tree - so the edit-observe detector is matching a path its own glob excludes, probably a Windows separator issue. Nothing was wrong with the file. NOT DONE, and deliberately: .claude/skills/run-kondo/fixture.mjs builds only one plugin, and that plugin ships a skill. The skill-less plugin needed for the empty-case check was added to the built tree by hand at run time and torn down after. A future session verifying that case will have to do the same unless fixture.mjs grows a second, skill-less plugin.\n2026-09-01 scope drift — touched but unpredicted: docs/adr/0007-stat-first-lazy-scanning.md, docs/domain.md, electron/main/ipc.ts, electron/main/workspace/kinds.ts, electron/main/workspace/workspace.ts, electron/preload/index.ts\n2026-09-01 lesson recorded: .foreman/notes.jsonl, area electron/main","model":"opus","effort":"high"}
{"id":"013","title":"Correct the sidebar label and the move collision message","why":"Found by running the app for 005 verification. src/app/app.tsx said read-only preview - v0.1 while three mutations ship, which tells a user their store is safe from kondo at the moment it can move their skills. And skillMovePlan built the collision refusal from clash.origin, the colliding skill own directory, so it read ...skills/db-migrate already holds a skill named db-migrate.","what":"Footer now reads every change is undoable - v0.1, which is the ADR-0001 promise rather than a false claim. The refusal now leads with the destination scope and keeps the colliding directory in parentheses.","status":"done","source":"claude-suggested","depends_on":["005"],"planned_touches":["src/app/app.tsx","electron/main/workspace/kinds.ts"],"observed_touches":["electron/main/workspace/kinds.ts","src/app/app.tsx"],"commits":["679ec9c"],"created_at":"2026-08-29","updated_at":"2026-08-29","notes":"2026-08-29 2026-08-29 Both text fixes shipped on main. Checks run: npm run typecheck exits 0, npm run lint exits 0, npm test 102/102 across 15 files, npm run guards reports No findings. No test asserted either string, so nothing needed updating; the collision refusal is still covered by test/skill-move.test.ts, which asserts on the will not merge fragment rather than the path.","model":"opus","effort":"low"}
{"id":"014","title":"Capture the fixture-run recipe as the run-kondo project skill","why":"I handed the 005 Move picker back to the user as an unverified visual check, then settled it myself in minutes once asked. The user pointed out I could always have done that, and asked for the recipe captured so no future session repeats the miss.","what":"A project skill at .claude/skills/run-kondo/ holding SKILL.md plus two scripts: fixture.mjs builds a synthetic store and prints the three locator env overrides, drive.mjs drives the running window over Chromium debugging port with open/shoot/press/pick/eval. No new dependency - Node 22 global WebSocket replaces Playwright.","status":"done","source":"user","depends_on":[],"planned_touches":[".claude/skills/run-kondo/"],"observed_touches":[".claude/skills/run-kondo/SKILL.md",".claude/skills/run-kondo/drive.mjs",".claude/skills/run-kondo/fixture.mjs",".gitignore"],"commits":["f0ba1e7"],"created_at":"2026-08-29","updated_at":"2026-08-29","notes":"2026-08-29 2026-08-29 Built and then used to check itself. fixture.mjs and drive.mjs both ran end to end: open Skills, pick a destination, read the screenshot. That run caught two things a static review would not have. First, out/ was stale from a build taken before commit 679ec9c, so the footer still showed the old string until a rebuild - the skill now says to build before launching. Second, drive.mjs pick matches the first row containing its argument, and a location substring matches several rows, so SKILL.md says to name the skill instead. Confirmed after rebuild: the footer reads every change is undoable - v0.1 and the collision refusal now leads with the destination scope. DEPENDENCY: none added. Node 22 ships a global WebSocket, so CDP needs no Playwright - the run skill fallback assumed Playwright and it was not necessary. GITIGNORE: .claude/* excluded everything but rules/, on the stated reasoning that the rest is per-machine. run-kondo is project knowledge rather than machine setup, so the user chose to share it; ij-debugger stays ignored because it needs a running IDE. The re-include of skills/ must precede the exclusion of its contents, since git will not look inside a directory it has already excluded - the first attempt got that order wrong and git check-ignore caught it. CHECKS RUN: npm run typecheck exits 0, npm run lint exits 0, npm test 102/102 across 15 files, npm run guards reports No findings. The PostToolUse jig hook flagged drive.mjs under the offline-by-design guard and fixture.mjs under the renderer-purity guard; both are false positives, and the real driver reports nothing - the guards scope to the app, and these are dev scripts.\n2026-08-29 scope drift — touched but unpredicted: .gitignore\n2026-08-29 lesson recorded: .foreman/notes.jsonl, area .claude/skills","model":"opus","effort":"medium"}
{"id":"015","title":"Write the tidy sweep plan doc","why":"CONTRIBUTING.md and AGENTS.md make a plan file the first commit of a feature, and docs/plans/README.md indexes them. Entry 006 shipped without one because docs/ sat outside its declared file surface, leaving eleven decisions recorded only in code comments.","what":"Add docs/plans/006-tidy-sweep.md following the template in docs/plans/README.md, recording the decisions the sweep settled: the fixed reclaimable-cache allowlist, the new 'store' EntityKind, empty-beats-stale for a zero-byte transcript, a sidecar riding with its transcript as one item, sidecar bytes uncounted under ADR-0007, and no journal entry on a no-op sweep. Add its index line to docs/plans/README.md.","status":"done","source":"claude-suggested","depends_on":[],"planned_touches":[],"observed_touches":["ROADMAP.jsonl","docs/plans/006-tidy-sweep.md","docs/plans/README.md"],"commits":["eec8777"],"created_at":"2026-08-29","updated_at":"2026-08-29","notes":""}
{"id":"016","title":"Commit .claude/settings.json so the jig guards travel with the repo","why":"The PostToolUse registration entry 008 added lived in a gitignored file (.gitignore, .claude/*), so a clone got .jig/hooks/session-guards.cjs with nothing invoking it and the guards ran only for whoever had already installed the jig plugin.","what":"Add !.claude/settings.json to .gitignore above the existing !.claude/rules/ line, rewrite the comment block that called everything under .claude/ per-machine, and commit the settings file. .claude/settings.local.json stays ignored as the machine-specific override.","status":"done","source":"claude-suggested","depends_on":[],"planned_touches":[".gitignore",".claude/settings.json"],"observed_touches":[".claude/settings.json",".gitignore"],"commits":["7028811"],"created_at":"2026-08-29","updated_at":"2026-08-29","notes":"2026-08-29 Done in the same session as 008, at the user's call. .gitignore now carries !.claude/settings.json above !.claude/rules/, and the comment block above it says settings.json is shared because it registers the jig session guards. Verified: git add --dry-run .claude/settings.json accepts the file, .claude/settings.local.json is still ignored, and npm run guards exits 0 with no findings over the staged change.","model":"opus","effort":"xhigh"}
{"id":"017","title":"Decide how kondo lives with jig guards that ignore their own path scope","why":"jig's hooks/jig-lib.js evalEdit reads params.patterns, stripComments, stripStrings and commentSyntax off a detector but never params.paths, so a check declaring paths:[src/**/*.ts] still denies edits anywhere. renderer-reaches-past-the-bridge therefore blocks legitimate node: imports under electron/main/ and test/. Now that entry 008 wired the runner into every session from the repo itself, this hits everyone, not just whoever had the plugin: .jig/ledger.jsonl already holds 47 deny rows, including electron/main/workspace/tidy.ts, test/tidy.test.ts and .claude/skills/run-kondo/drive.mjs.","what":"The defect is upstream in the slag repo (jig hooks/jig-lib.js evalEdit), not in kondo's own checks, and this entry does not fix it there. Decide what kondo does meanwhile. zones.observe in .jig/config.json is global across all four guards, so it cannot silence one guard under electron/ while leaving outbound-network-call and raw-path-across-the-seam armed there. The realistic options are holding renderer-reaches-past-the-bridge and test-touches-a-real-store in observe via their mode field, narrowing their patterns so they only match what the paths field meant, or waiting on the upstream fix. Do not change what the checks detect without settling which.","status":"done","source":"claude-suggested","depends_on":[],"planned_touches":[".jig/config.json",".jig/checks/renderer-reaches-past-the-bridge.check.mjs",".jig/checks/test-touches-a-real-store.check.mjs"],"observed_touches":[".jig/config.json"],"commits":["89836e47d130e5dcd1fa6ce0a58411d67c38046d"],"created_at":"2026-08-29","updated_at":"2026-09-01","notes":"2026-09-01 DECISION: option (a) - hold renderer-reaches-past-the-bridge and test-touches-a-real-store in observe via their own mode field in .jig/config.json. No check file was touched; both proof hashes stay valid. WHY IT WINS. Verified the defect first: jig 2.7.1 hooks/jig-lib.js evalEdit (line 623) reads params.patterns/stripComments/stripStrings/commentSyntax and never params.paths. But .jig/checks/run.mjs DOES honour paths (line 424, matchesAny(rel, p.paths)), and that runner is what the pre-commit hook and .github/workflows/ci.yml line 30 execute. So the real invariant - no node:/electron import under src/ - keeps full BLOCKING coverage in the commit and CI lanes. Observe only relaxes the one lane that is broken, and the guard still fires there: effectiveState returns observe -> decision would-deny, still written to .jig/ledger.jsonl, so evidence for the upstream bug keeps accruing. Option (b) narrow the patterns was rejected as unbuildable, not merely worse: patterns match file CONTENT, paths matches file LOCATION, and no content regex can express 'only under src/'. To stop renderer-reaches-past-the-bridge firing on electron/main/ you would have to delete the \bfrom[\"']node: pattern, which is the whole substance of the check - and that loss would propagate to the checks/pre-commit/CI lane too, where the guard currently works correctly. (b) trades a working lane for a broken one. Option (c) wait and change nothing was rejected on cost: entry 008 wired the runner into every session from the repo, so this now denies every contributor. 53 deny rows in .jig/ledger.jsonl; renderer-reaches-past-the-bridge alone denied 11 distinct real files including electron/main/workspace/{tidy,mutations,kinds,workspace}.ts, 7 test/*.test.ts files and .claude/skills/run-kondo/drive.mjs. VERIFIED. node .jig/checks/run.mjs -> 'No findings.' exit 0. npm test -> 18 files, 130 tests passed. npm run lint (oxlint) -> exit 0. Invariants proved directly by driving jig-lib's own evaluateGuard + effectiveState against the edited config (scratchpad probe, no ledger writes): electron/main/workspace/tidy.ts + node:fs  -> renderer-reaches-past-the-bridge would-deny (was deny) test/tidy.test.ts + node:os/homedir()      -> renderer would-deny, test-touches-a-real-store would-deny src/net.ts + fetch(                        -> outbound-network-call DENY (unchanged) src/panel.tsx + node:fs                    -> raw-path-across-the-seam DENY (unchanged) FINDING, not fixed here (constraints forbid touching those two guards). All FOUR guards carry the defect, not two. raw-path-across-the-seam declares paths [electron/preload/**/*.ts, electron/main/ipc.ts] but its \bfrom[\"']node:fs[\"'] pattern denies any edit to the six electron/main/workspace files that import node:fs (jsonl, mutations, scan, sessions, user-store, workspace); the ledger already records denies on electron/main/workspace/workspace.ts plus three test/*.test.ts files. outbound-network-call declares [src/**, electron/**, shared/**] but denied .claude/skills/run-kondo/drive.mjs. So editing electron/main/workspace/*.ts is still blocked after this change - by a different guard. Worth its own entry. WHERE THE DECISION LIVES. .jig/config.json takes no comments and jig warns on unknown top-level keys (CONFIG_KEYS, line 167), so the reasoning is recorded here and in commit 89836e4 rather than in the config. An ADR under docs/adr/ would be the repo's own convention for a kind:decision entry, but that is outside this task's stated file surface and was not written unasked. No unverified checks - every bar was answerable by command.\n2026-09-01 scope drift — predicted but untouched: .jig/checks/renderer-reaches-past-the-bridge.check.mjs, .jig/checks/test-touches-a-real-store.check.mjs\n2026-09-01 lesson recorded: .foreman/notes.jsonl, area .jig","kind":"decision","model":"opus","effort":"high"}
{"id":"018","title":"Undo of a write step can strand the file when the step never ran","why":"Entry 010 made undo tolerant of a mutation that failed part way, but guarded only the move and trash reversals. The write reversal in undo() still assumes its step ran. runSteps copies the target to trashPath(journalId, step.displaced) and only then writes the new content, so a failure between those two leaves the displaced bytes absent. undo() then sees the target present, relocates it into the undo trash, and throws on fs.cp from the absent trash path — the store loses the file to a trash directory and the undo reports failure.","what":"Guard the write reversal in electron/main/workspace/mutations.ts undo() the way 010 guarded move and trash: do not displace the target unless the bytes that would come back are actually there. The open decision is honesty, which is why 010 left it: an absent trash path also means the trash was emptied, and skipping silently would drop the emptied-trash message for a write-only entry (the message currently produced by the isEnoent branch in undo()). Decide between a per-step signal in the returned Scan errors and accepting the silence. Cover it in test/mutation.test.ts alongside the three cases 010 added, using the same fsp.rename spy pattern, and keep the existing case reports an emptied trash rather than silently skipping a lost restore green.","status":"done","source":"claude-suggested","depends_on":[],"planned_touches":["electron/main/workspace/mutations.ts","test/mutation.test.ts"],"observed_touches":["docs/domain.md","electron/main/workspace/mutations.ts","test/mutation.test.ts"],"commits":["d342d71"],"created_at":"2026-09-01","updated_at":"2026-09-02","notes":"2026-09-02 correction applied: planned_touches\n2026-09-02 undo()'s write branch in electron/main/workspace/mutations.ts now resolves the displaced trash path first and stats it before anything moves; only then does it relocate the target into the undo's own trash and restore. Verification ran red-then-green rather than the prescribed green-then-break order: the two new cases in test/mutation.test.ts fail against the unmodified undo() (settings.json ends up in the undo trash and the store hash differs), and pass after the guard - stronger evidence than breaking a passing test. Honesty decision, recorded in a comment at the branch: a per-step signal in the returned Scan errors was rejected. A move or a trash tells a never-ran step from an emptied trash by its source; a write cannot, because its target is present either way. The stat raises the ENOENT that already becomes the emptied-trash refusal, so a write-only entry keeps that message and the store is untouched either way; a silent skip would report an undo that succeeded and put nothing back. Cost, accepted: a write step that never ran is reported as an emptied trash, which is imprecise - but the pre-existing behaviour reported the same thing while also stranding the file, so nothing regressed. One file beyond the declared surface, approved by the user mid-task: docs/domain.md line 291-298, whose bullet claimed undo skips any step whose effect is absent - now false for a write. The jig guard workspace-adapter-outruns-domain-doc blocks any electron/main/workspace/ commit without it. Ran: npm test -- test/mutation.test.ts (31 pass), npm run typecheck (exit 0), npm test (191 pass, 21 files), npm run lint (exit 0). The three entry-010 cases and the emptied-trash case stay green. No unverified checks: the change is undo internals and the user-facing refusal text is unchanged, so there is nothing new to look at in the UI.\n2026-09-02 scope drift — touched but unpredicted: docs/domain.md\n2026-09-02 lesson recorded: .foreman/notes.jsonl, area docs","model":"opus","effort":"high"}
{"id":"019","title":"jig renderer guard fires on files outside src/","why":"The PostToolUse renderer-reaches-past-the-bridge guard blocked a Write to test/plugin-skills.test.ts, reporting \"src/ is the renderer\". Its params.paths is [\"src/**/*.ts\",\"src/**/*.tsx\"] and node .jig/checks/run.mjs reports No findings over the same tree, so the edit-observe detector matches paths its own glob excludes. Every session that writes a .ts file outside src/ pays a spurious block.","what":"Reproduce by writing any new .ts file under test/. The check module is .jig/checks/renderer-reaches-past-the-bridge.check.mjs; the two PostToolUse runners are jig hooks/runner.js and .jig/hooks/session-guards.cjs, and both fired identically, so the bug is likely in the shared glob-to-path matching rather than the check. Suspect Windows backslash separators not normalised before the src/** match. Fix the matcher, then confirm a write under test/ passes and a genuine node: import under src/ still blocks.","status":"dropped","source":"claude-suggested","depends_on":[],"planned_touches":[],"observed_touches":[],"commits":[],"created_at":"2026-09-01","updated_at":"2026-09-01","notes":"2026-09-01 Superseded by entry 017, which settled kondo's answer to this exact defect (commit de18dab). ROOT CAUSE CORRECTION. 019's hypothesis - 'Windows backslash separators not normalised before the src/** match' - is wrong. Verified by reading jig 2.7.1 hooks/jig-lib.js: evalEdit (line 623) reads params.patterns, stripComments, stripStrings, commentSyntax and onlyWhenIntroduced, and never reads params.paths at all. There is no glob match to normalise; the scope field is simply never consulted on the PostToolUse lane. The path is present there as `rel` (line 626) and is passed to blankRegions for comment-syntax selection, then dropped. globToRegExp (line 486) already exists in that same file and is already used against a path by zoneForcesObserve (line 515). NOT FIXABLE FROM KONDO. .jig/hooks/session-guards.cjs line 62 resolves the newest installed jig under ~/.claude/plugins and delegates to its hooks/runner.js. Nothing in this repo implements the matcher. When the upstream fix ships, 'newest version wins' means kondo picks it up with no change here. WHAT WAS DONE INSTEAD. Entry 017 held renderer-reaches-past-the-bridge and test-touches-a-real-store in observe via their mode field in .jig/config.json, keeping the blocking coverage in the checks/pre-commit/CI lane, which does honour paths (.jig/checks/run.mjs line 424). UPSTREAM. A full bug ticket for the slag repo was drafted this session: scratchpad jig-paths-ticket.md. It carries the reproduction, the fix location, the back-compat and absolute-path edge cases, and the note that zones.observe likely mismatches absolute paths for the same reason.","model":"opus","effort":"high"}
{"id":"020","title":"Give the run-kondo fixture a plugin that ships no skills","why":"Entry 012 added a per-plugin skills listing whose empty case renders \"Ships no skills.\". fixture.mjs builds exactly one plugin (foreman@acme) and it ships a skill, so verifying that case meant hand-editing X:/Temp/kondofix after the build. The skill file says the tree is shaped to exercise the awkward cases; this one is now missing.","what":"In .claude/skills/run-kondo/fixture.mjs, add a second entry to installed_plugins.json - e.g. hush@acme at plugins/cache/acme/hush/1.0.0 - and write that install root with a commands/ file and no skills/ directory, so the plugins view has a row that expands to the empty state. Also consider a second skill under the existing pluginInstall so the populated listing has more than one row. Update the Fixture notes section of SKILL.md to say what the new plugin is for.","status":"done","source":"claude-suggested","depends_on":[],"planned_touches":[".claude/skills/run-kondo/fixture.mjs",".claude/skills/run-kondo/SKILL.md"],"observed_touches":[".claude/skills/run-kondo/fixture.mjs",".claude/skills/run-kondo/SKILL.md"],"commits":["5b91e481ae00dff69e8d5ec56227b13cfbbc0748"],"created_at":"2026-09-01","updated_at":"2026-09-03","notes":"2026-09-03 correction applied: planned_touches\n2026-09-03 scope drift — predicted but untouched: .claude/skills/run-kondo/SKILL.md\n2026-09-03 2026-09-03 fixture.mjs now writes hush@acme (marketplace acme, version 1.0.0) into installed_plugins.json with installPath plugins/cache/acme/hush/1.0.0, and builds that root with commands/hush.md and no skills/ directory. foreman@acme gains a second shipped skill (survey) alongside roadmap. Both install roots are written with the same await write(root, {...}) helper the existing one uses; no second mechanism was added. SKILL.md Fixture notes now names hush@acme as the empty case and warns against adding skills/ there to tidy the tree. PREMISE CORRECTED. The task said to verify via the plugins view expanding to \"Ships no skills.\". That view no longer exists and that string is nowhere in the renderer. src/app/app.tsx offers exactly three destinations - Projects, Clean up, History - confirmed live on the running app (nav buttons read [\"Projects\",\"Clean up\",\"History\"]). grep for \"no skills\" across ts/tsx/md hits only test/plugin-skills.test.ts. grep for pluginSkills across src/ returns nothing: the seam method has no renderer consumer at all. plugin-control.tsx line 17 calls the old arrangement \"the old plugins view\" in its own comment; its only disclosure is a settings-layer strip labelled layers, not a skills listing. VERIFIED INSTEAD, all run here, nothing handed back. (1) node .claude/skills/run-kondo/fixture.mjs built clean; installed_plugins.json holds both foreman@acme 2.3.0 and hush@acme 1.0.0, and find over plugins/cache shows foreman/2.3.0/skills/{roadmap,survey}/SKILL.md and hush/1.0.0/commands/hush.md with no hush skills directory. (2) The app was built and launched against that fixture and driven over CDP. Projects view renders \"Plugins 2\" with both rows - foreman On / in effect: on, hush Not set / in effect: nothing says - screenshot read, not just asserted. (3) The empty state itself was proved across the real preload seam rather than in a unit test: window.kondo.pluginSkills(\"plugin:hush@acme\") returns {data:[],errors:[]} and pluginSkills(\"plugin:foreman@acme\") returns [\"roadmap\",\"survey\"]. That is exactly the assertion test/plugin-skills.test.ts:77 makes against its own synthetic tree, now true of the shipped fixture. (4) The second plugin skill did not leak into the user catalogue: the Skills section still reads 3 (old-linter, api-notes, commit-writer). (5) npm run typecheck exit 0, npm run lint exit 0, node .jig/checks/run.mjs No findings, npm test 21 files / 191 tests passing. FINDING, not fixed here. SKILL.md Drive section still teaches commands the app cannot answer: \"node drive.mjs open Skills\" names a tab that does not exist, and \"node drive.mjs pick commit-writer apiserver\" describes reaching a skill through a Skills tab rather than through the Projects view. Fixing that is a different defect from this entry (the app restructure orphaned the doc) and the task scoped SKILL.md to its Fixture notes section, so it was left alone and offered to the user as a roadmap candidate. FINDING. The \"Ships no skills.\" empty state named in this entry why is not reachable by a user today - pluginSkills is wired through preload and ipc but nothing in src/ calls it. The fixture is now ready for whoever builds that listing; until then the case lives at the seam only. No unverified lines: every check this entry earns had a command, and the one that had no command had no UI to look at either.\n2026-09-03 scope drift — predicted but untouched: .claude/skills/run-kondo/fixture.mjs\n2026-09-03 lesson recorded: .foreman/notes.jsonl, area .claude/skills","model":"opus","effort":"high"}
{"id":"021","title":"Decide the same path-scope answer for raw-path-across-the-seam and outbound-network-call","why":"Entry 017 settled the jig path-scope defect for two of the four guards, but all four carry it: jig 2.7.1 hooks/jig-lib.js evalEdit never reads params.paths on the PostToolUse lane. raw-path-across-the-seam declares paths [electron/preload/**/*.ts, electron/main/ipc.ts] yet its \bfrom[\"']node:fs[\"'] pattern denies any edit to the six electron/main/workspace files that import node:fs (jsonl, mutations, scan, sessions, user-store, workspace). .jig/ledger.jsonl already records denies on electron/main/workspace/workspace.ts and three test/*.test.ts files. outbound-network-call declares [src/**, electron/**, shared/**] but denied .claude/skills/run-kondo/drive.mjs. So editing electron/main/workspace/*.ts is still blocked after 017.","what":"017's constraints explicitly barred touching these two, so the same decision has to be taken for them deliberately rather than by extension. Weigh the same three options 017 weighed and reuse its reasoning where it holds: (a) mode observe in .jig/config.json, which keeps full blocking coverage in the checks/pre-commit/CI lane because .jig/checks/run.mjs line 424 does honour paths; (b) narrow patterns, which 017 rejected as unbuildable since patterns match content and paths matches location; (c) wait for upstream. Note the asymmetry that may change the answer here: raw-path-across-the-seam guards the preload seam, a narrower and higher-value surface than the renderer guard, so the cost of observing it is not identical. Do not edit the check files - both carry proof hashes in .jig/config.json. Verify with node .jig/checks/run.mjs, npm test and npm run lint, and prove the before/after decisions by driving jig-lib's evaluateGuard and effectiveState directly rather than by writing real files.","status":"done","source":"claude-suggested","depends_on":["017"],"planned_touches":[".jig/config.json"],"observed_touches":[".jig/config.json"],"commits":[],"created_at":"2026-09-01","updated_at":"2026-09-03","notes":"2026-09-03 2026-09-03 DECISION: option (a) for BOTH guards - mode observe in .jig/config.json for raw-path-across-the-seam and outbound-network-call. Two-line diff, mode field only. Neither check file touched; both proof hashes still match the checks on disk (checkProof == config.proof, verified for both). SAME ANSWER AS 017, DIFFERENT REASONING. 017 argued from the defect's shape. Here the ledger settles it empirically: across 615 rows in .jig/ledger.jsonl, raw-path-across-the-seam has 34 PostToolUse denies and outbound-network-call has 5, and ZERO of the 39 fall inside the guard's own declared params.paths (tested by running each denied path through jig-lib's own globToRegExp against the check's paths). Every real deny was a false positive: electron/main/workspace/{workspace,projects,sessions}.ts, test/{helpers,skill-toggle,plugin-toggle,skill-move}.test.ts, shared/contract.ts, .claude/skills/run-kondo/drive.mjs (a localhost CDP fetch, deliberately outside src|electron|shared) and six scratchpad .mjs files. THE ASYMMETRY DOES NOT CHANGE THE ANSWER. The entry asked whether raw-path-across-the-seam is different because it guards the preload seam - narrower and higher value than the renderer guard. It is higher value, but the session lane cannot see that surface: evalEdit (jig-lib.js line 623) reads params.patterns/stripComments/stripStrings/commentSyntax and never params.paths, so keeping it armed does not buy protection of electron/preload/** specifically - it buys blocking on every file in the repo that imports node:fs or declares a *Path: string field. The seam's actual protection is bought in the checks/pre-commit/CI lane, which is intact either way. A narrow high-value scope is only worth arming for if the lane can read the scope. It cannot. Value of the armed session lane = earlier warning on 2 files it has never once fired on; cost = blocking work on 10+ files where it is simply wrong. OPTION (b) NARROW THE PATTERNS - rejected, same ground as 017 and it holds here unchanged: patterns match file CONTENT, paths matches file LOCATION, and no content regex expresses 'only under electron/preload/'. Narrowing raw-path-across-the-seam would mean deleting its node:fs import pattern, which is half the check's substance, and the loss would propagate into the checks/pre-commit/CI lane where the guard currently works correctly. Trading a working lane for a broken one. OPTION (c) WAIT FOR UPSTREAM - rejected on cost, sharper than in 017: raw-path-across-the-seam blocks the six electron/main/workspace files that import node:fs (jsonl, mutations, scan, sessions, user-store, workspace), which is where kondo's adapter work actually happens, and it blocks scratchpad probe scripts, which is how these very decisions get proved. OPTION (d) ZONES - considered and rejected as unbuildable, not previously written down. zones.observe would in principle emulate paths semantics, but it is an allow-list of locations to WEAKEN and globs cannot express negation, so covering 'everything except electron/preload/**' means enumerating the repo and silently breaking on every new directory. Worse, zones are global across all guards: one zone list cannot weaken raw-path-across-the-seam on src/** and shared/** while leaving outbound-network-call armed there, and src|shared is exactly outbound-network-call's real surface. One global knob cannot serve two different scopes. VERIFIED BY COMMAND. node .jig/checks/run.mjs -> 'No findings.' exit 0. npm test -> 21 files, 191 tests passed. npm run lint (oxlint) -> exit 0. node .jig/checks/run.mjs --selftest -> every runnable check caught its own violation, including 'caught outbound-network-call - seeded src/fixture.ts, 3 hits' and 'caught raw-path-across-the-seam - seeded electron/preload/fixture.ts, 3 hits' - direct proof that stepping the PostToolUse guards to observe costs nothing in the lane the pre-commit hook and .github/workflows/ci.yml line 30 run. INVARIANTS PROVED BY DRIVING jig-lib DIRECTLY (scratchpad probe021.cjs, in-process, no file written to provoke a guard, no ledger row appended): raw-path-across-the-seam on electron/main/workspace/workspace.ts + node:fs import -> deny BEFORE, would-deny AFTER; outbound-network-call on .claude/skills/run-kondo/drive.mjs + fetch( -> deny BEFORE, would-deny AFTER. Both in-scope controls (electron/preload/index.ts with a filePath: string param; src/net.ts with fetch() also move deny -> would-deny, which is the honest cost of the change and is the cost the checks lane covers. effectiveState returns why='not asked to arm' in every after case, so the guard still fires and still writes evidence to .jig/ledger.jsonl for the upstream bug. PROMPT MISMATCH. The entry description flagged evaluateGuard and effectiveState as possibly invented or renamed. They are real: both are defined in jig 2.7.1 hooks/jig-lib.js (lines 653 and 526) and both are in its module.exports (lines 806-807). No rename. The description also renders outbound-network-call's declared paths as [src/**, electron/**, shared/**]; on disk they are the narrower [src/**/*.ts, src/**/*.tsx, electron/**/*.ts, shared/**/*.ts], which does not change the conclusion - drive.mjs is out of scope under either reading. FINDING, not acted on. All four PostToolUse guards now sit in observe, so kondo currently has no armed session-lane guard at all. That is the correct state while the upstream defect stands, but it means the pre-commit hook and CI are the only enforcement; if either is ever bypassed the invariants are unguarded. Re-arming all four is one config edit once jig's evalEdit honours params.paths - worth its own entry when that lands. Also, as in 017, an ADR under docs/adr/ would be this repo's own convention for a kind:decision entry, but that is outside this task's stated file surface and was not written unasked. No unverified checks - every bar was answerable by command.\n2026-09-03 lesson recorded: .foreman/notes.jsonl, area .jig","kind":"decision","model":"opus","effort":"high"}
{"id":"022","title":"Resolve project paths through the ~/.claude.json registry","why":"Every per-project feature keyed on un-flattening ~/.claude/projects names, which resolved 7 of 9,171 directories on the owner's machine and can never name a project with a hyphen in its path. The owner's vision (project-first view, moving things between projects, dead-project cleanup) has no foundation without a correct answer to which directory a project is.","what":"Locator names ~/.claude.json beside the user store; projects.ts flattens its projects keys with Claude's rule ([^A-Za-z0-9] -> -) into an index consulted before the fallback guess; sessions.ts reads it once per inventory and keeps only the keys. ADR-0009 records the decision. Fixture helper registerProjects writes a fixture registry; boundary and workspace suites drop their TMP_OK gates.","status":"done","source":"user","depends_on":[],"planned_touches":["electron/main/workspace/projects.ts","electron/main/workspace/sessions.ts","electron/main/workspace/locator.ts","test/","docs/adr/0009-projects-come-from-claudes-registry.md","docs/domain.md"],"observed_touches":["electron/main/workspace/projects.ts","electron/main/workspace/sessions.ts","electron/main/workspace/locator.ts","electron/main/workspace/workspace.ts","test/helpers.ts","test/projects.test.ts","test/sessions.test.ts","test/boundary.test.ts","test/workspace.test.ts","test/locator.test.ts","docs/adr/0009-projects-come-from-claudes-registry.md","docs/domain.md",".foreman/notes.jsonl","CHANGELOG.md","README.md","ROADMAP.jsonl","ROADMAP.md","SECURITY.md","docs/adr/0006-native-conventions-over-invented-state.md","docs/adr/0007-stat-first-lazy-scanning.md","docs/adr/0008-composite-identity-ids-across-the-seam.md","docs/adr/README.md","docs/foundations.md","docs/glossary.md","docs/plans/v1-read-only-core.md","docs/testing.md","shared/contract.ts"],"commits":["121df70"],"created_at":"2026-09-02","updated_at":"2026-09-02","notes":"2026-09-02 Shipped with the direction audit. Also drops the inventory cache after a sweep or undo whether or not it finished. Uncommitted at the time of writing.\n2026-09-02 scope drift — touched but unpredicted: .foreman/notes.jsonl, CHANGELOG.md, README.md, ROADMAP.jsonl, ROADMAP.md, SECURITY.md, docs/adr/0006-native-conventions-over-invented-state.md, docs/adr/0007-stat-first-lazy-scanning.md, docs/adr/0008-composite-identity-ids-across-the-seam.md, docs/adr/README.md, docs/foundations.md, docs/glossary.md, docs/plans/v1-read-only-core.md, docs/testing.md, electron/main/workspace/workspace.ts, shared/contract.ts"}
{"id":"023","title":"Read-only kind for MCP servers at user, local and project scope","why":"MCP declarations are the first thing the owner named for cleanup and nothing in kondo reads them. They live in three native places: ~/.claude.json mcpServers (user), ~/.claude.json projects[path].mcpServers plus disabledMcpServers (local), and <project>/.mcp.json (project) — the last sits at the project root, outside ADR-0002 as written.","what":"Amend ADR-0002 to name <project>/.mcp.json as the one Claude-owned file kondo may open outside .claude, and pin the list in boundary.test.ts. Add EntityKind mcp with ids mcp:user:<name>, mcp:local:<flat>/<name>, mcp:project:<flat>/<name>; discover is tier-1 (the entry-022 registry parse plus one .mcp.json read per verified project); each entity carries orphan=true when its owning path no longer exists. Capability rows refuse every operation until entry 031. Never surface env or headers values. domain.md rows and a fixture builder for both files.","status":"done","source":"user","depends_on":["022"],"planned_touches":["shared/contract.ts","electron/main/workspace/kinds.ts","electron/main/workspace/capabilities.ts","electron/main/workspace/user-store.ts","docs/adr/0002-project-privacy-boundary.md","docs/domain.md","test/helpers.ts","test/boundary.test.ts"],"observed_touches":["docs/adr/0002-project-privacy-boundary.md","docs/domain.md","electron/main/workspace/capabilities.ts","electron/main/workspace/kinds.ts","electron/main/workspace/user-store.ts","shared/contract.ts","test/boundary.test.ts","test/helpers.ts","test/mcp.test.ts"],"commits":[],"created_at":"2026-09-02","updated_at":"2026-09-02","notes":"2026-09-02 Shipped. EntityKind 'mcp' + McpScope/McpServerInfo in shared/contract.ts; scanMcpServers in user-store.ts does one ~/.claude.json parse (user mcpServers, then projects[path].mcpServers with disabledMcpServers) plus one .mcp.json read per verified project gated by disabledMcpjsonServers; kinds.ts registers the kind with noPlanYet seats; capabilities.ts adds an mcp row refusing enable/disable/move in all three scopes with a reason naming entry 031. Ids are mcp:user:<name>, mcp:local:<flat>/<name>, mcp:project:<flat>/<name>. orphan=true only where it can be true: a registry project path that fails a stat; user and project scope declarations were read from a file that exists, so they are never orphan. The registry stat is paid only by an entry that actually declares a server, so a 4000-project registry costs as many stats as it has MCP users (ADR-0007). Only name/transport/source/scope/project/enabled/orphan cross the seam - nothing is built from env or headers, values or key names, and test/mcp.test.ts asserts the serialized result contains neither the secret nor the key name. ADR-0002 amended with the amendment section, a rejected option, and a consequence naming the pinned list; docs/domain.md gained a three-scope MCP table plus the note that ~/.claude/.mcp.json is not one of them. test/helpers.ts gained mcpServer() (which always carries a secret, so suites assert on its absence) and registerMcp(). Verification, all run here: npm run typecheck exit 0, npm run lint exit 0, npm test 19 files / 143 tests passing. The boundary pin was proved twice by deliberate widening: adding a CLAUDE.md read fails the allowed() guard, and adding it to allowed() as well still fails the new literal pin with the extra path - both reverted, tree clean. No unverified lines: the mcp kind has no UI to look at yet (entry 026 builds the project view that surfaces it), so every check this entry earns had a command. Findings: (1) test/mcp.test.ts is one file beyond the stated surface; the user chose it over folding the cases into an existing suite. (2) No KondoApi method or channel was added - the entry's file surface excludes ipc.ts and preload, and entry 026 owns the seam method, so today the kind is reachable only through kinds.mcp.discover(). (3) 'local' scope covers every registered project including unverified and dead ones, while 'project' scope covers only verified projects; that asymmetry is deliberate - .mcp.json cannot be read from a path that is not there.\n2026-09-02 scope drift — touched but unpredicted: test/mcp.test.ts\n2026-09-02 lesson recorded: .foreman/notes.jsonl, area electron/main","model":"opus","effort":"high"}
{"id":"024","title":"Read-only kinds for agents, commands, rules and output styles","why":"The owner wants to promote an agent or something similar from a project to global, and to see which are tied to each project. None has a kind; only skills/ is read from a project store although agents/, rules/ and hooks/ were observed in every sampled one.","what":"A placed-entry reader in user-store.ts parameterised by directory and shape (skill dir with SKILL.md; single .md file with frontmatter) covering ~/.claude/{agents,commands,rules,output-styles} and <project>/.claude/{agents,commands,rules}. EntityKind members agent, command, rule, output-style with ids <kind>:user:<name> and <kind>:project/<flat>:<name>; matrix rows deny enable/disable with a stated reason (no native disable convention) and deny move until entry 028. Fixture builders and boundary sweep coverage.","status":"done","source":"user","depends_on":["022"],"planned_touches":["shared/contract.ts","electron/main/workspace/kinds.ts","electron/main/workspace/capabilities.ts","electron/main/workspace/user-store.ts","docs/domain.md","test/helpers.ts"],"observed_touches":["docs/adr/0006-native-conventions-over-invented-state.md","docs/domain.md","electron/main/workspace/capabilities.ts","electron/main/workspace/kinds.ts","electron/main/workspace/user-store.ts","shared/contract.ts","test/boundary.test.ts","test/helpers.ts","test/placed.test.ts"],"commits":["2cf2e81","7ffb072"],"created_at":"2026-09-02","updated_at":"2026-09-02","notes":"2026-09-02 scope drift — predicted but untouched: test/helpers.ts; touched but unpredicted: docs/adr/0006-native-conventions-over-invented-state.md\n2026-09-02 Shipped. One reader, readPlacedDir(locator, root, shape, c) in user-store.ts, takes the shape as a parameter: skill-dir reads <name>/SKILL.md, markdown reads <name>.md. scanSkills goes through it too (readSkillDir is now a 12-line mapper), so there are no near-copies. scanPlacedEntries(locator, kind, projects, c) drives all four kinds off a PLACED_DIRS table. Ids are <kind>:user:<name> and <kind>:project/<flat>:<name>; the name is the filename without .md, never the frontmatter name - the two can disagree and only the filename is unique in a directory. projectId carries attribution (ADR-0008). kinds.ts gained agent/command/rule/outputStyle from one placedKind(kind) factory. No API method and no renderer surface: like mcp before entry 026, these are registry-only, discovered and read but not yet exposed over IPC. Nothing renders, so there is nothing to look at by eye. Matrix: enable and disable denied with the ADR-0006 reason (Claude loads these by presence and ships no .disabled sibling and no settings key), move denied until entry 028. output-style has a user row only - no project store was observed carrying output-styles, so kondo does not go looking. Verified: npm run typecheck exit 0; npm test 20 files / 158 tests all passing (test/placed.test.ts adds 8); npm run lint exit 0. No unverified checks - this entry has no UI. Scope note, flagged after the fact rather than before: three files beyond the entry planned_touches. test/placed.test.ts and test/boundary.test.ts were required by the entry own verification bar (new cases for the four kinds, both shapes, the denials; extend the boundary sweep) but were not in planned_touches. docs/adr/0006 was forced by the jig seam-contract-outruns-its-adr guard, which blocks any commit touching shared/contract.ts without a docs/adr/**/*.md staged alongside; the paragraph added is a real consequence of this work, not a token edit. Finding: entry planned_touches for this kind of work should include test files, and every entry touching shared/contract.ts also needs a docs/adr touch to clear the pre-commit guard. Worth folding into how entries are drafted rather than discovered at commit time each run. Finding, for entry 028: skillPlacement in kinds.ts derives the store from the id by slicing around entity.name, which works only because a skill id is skill:<key>:<name>. The placed ids share that shape, so the generalised placement table can reuse the slice - but PLACED_DIRS lives in user-store.ts, and 028 will need it exported or moved beside the placement table.\n2026-09-02 scope drift — predicted but untouched: shared/contract.ts, electron/main/workspace/kinds.ts, electron/main/workspace/capabilities.ts, electron/main/workspace/user-store.ts, docs/domain.md; touched but unpredicted: test/boundary.test.ts, test/placed.test.ts\n2026-09-02 lesson recorded: .foreman/notes.jsonl, area electron/main","model":"opus","effort":"high"}
{"id":"025","title":"Carry project attribution on the seam and the project set as a union","why":"A per-project page must join skills, hooks, plugin layer states and settings layers to a project without parsing ids (ADR-0008). Today SkillInfo and HookInfo carry no project field, PluginScopeState.project is a folder basename two projects can share, and a plugin's winning layer is computed across all projects at once. The project list is only the ~/.claude/projects directory list.","what":"Add projectId: string | null to SkillInfo, HookInfo, PluginScopeState and SettingsLayerInfo, populated in user-store.ts; rename PluginScopeState.project to projectLabel. Replace PluginInfo.winningLayerId with effectiveIn per project (local > project > user). Add hasStore and path-exists to SessionProject, define the project set as the union of registry keys and projects/ directories with a sources field, and filter the skill-move picker on hasStore with a bad-request refusal naming the missing .claude. Note the attribution rule in ADR-0008.","status":"done","source":"user","depends_on":["022"],"planned_touches":["shared/contract.ts","electron/main/workspace/user-store.ts","electron/main/workspace/sessions.ts","electron/main/workspace/workspace.ts","src/features/plugins/plugins.tsx","src/features/skills/skills.tsx","docs/adr/0008-composite-identity-ids-across-the-seam.md","docs/domain.md"],"observed_touches":["docs/adr/0008-composite-identity-ids-across-the-seam.md","docs/domain.md","electron/main/workspace/sessions.ts","electron/main/workspace/user-store.ts","electron/main/workspace/workspace.ts","shared/contract.ts","src/features/plugins/plugins.tsx","src/features/skills/skills.tsx","test/plugin-toggle.test.ts","test/sessions.test.ts","test/skill-move.test.ts","test/user-store.test.ts",".foreman/notes.jsonl","ROADMAP.jsonl"],"commits":["d5d3cbb"],"created_at":"2026-09-02","updated_at":"2026-09-02","notes":"2026-09-02 Shipped. projectId: string | null now rides on SkillInfo, HookInfo, SettingsLayerInfo and PluginScopeState, populated in user-store.ts from the verified project's flattened dirName as project:code:<flat>; PluginScopeState.project became projectLabel (display only) and plugins.tsx groups on projectId instead of the folder name. PluginInfo.winningLayerId is replaced by effectiveIn: one {projectId, layerId, enabled} per project (its local, then its project, then the shared user layer) plus one for the user scope; empty when no layer mentions the plugin, which is what the 'enabled nowhere' pill now reads. scanSessionInventory unions the ~/.claude.json registry keys with the projects/ directories on the flattened path; each ProjectRecord/SessionProject carries sources ('registry' | 'transcripts'), pathExists and hasStore. skills.tsx filters the move picker on hasStore, and workspace.skillMove refuses a store-less destination as bad-request naming the missing .claude. ADR-0008 and docs/domain.md record all of it. Two deliberate departures from the brief. (1) guessedPath's meaning changed: a registry key IS the path (ADR-0009), so it is now kept even when the directory is gone, and pathExists carries the truth. Left as-is it would have been redundant with guessedPath !== null and carried no information, and the 52 dead registry keys domain.md names would have shown a blank path. (2) The refusal lives in workspace.skillMove rather than in kinds.ts skillMovePlan, which keeps kinds.ts off the changed-file list; moveTarget still answers unknown-id for a destination that is not a project at all. File surface beyond the brief's eight: test/sessions.test.ts, test/user-store.test.ts, test/plugin-toggle.test.ts and test/skill-move.test.ts. The verification block required new cases, and plugin-toggle.test.ts asserted on the two renamed/removed fields so it could not compile untouched. Nothing else was written. Verified by running, not by assertion. npm run typecheck exits 0; npm test is 19 files / 150 tests, all passing (7 new cases: transcripts-only attribution, the four union cases in sessions.test.ts, the projectId case in user-store.test.ts, the store-less refusal in skill-move.test.ts); npm run lint exits 0; node .jig/checks/run.mjs reports no findings. Then the built app was driven against a synthetic fixture (run-kondo skill) with a third project registered in ~/.claude.json holding no .claude: the Skills move picker offered only 'user' plus the two projects with a store, sessionProjects reported nostore as sources ['registry'], pathExists true, hasStore false, and skillMove into it returned bad-request naming X:\\Temp\\kondofix\\work\\nostore\\.claude with nothing written. On the Plugins tab, with the user layer enabling the plugin, the star sat on the global chip alone; after disabling it in apiserver's project layer the star appeared on that chip too while website kept falling through to the user layer — per-project resolution rendering as intended. The Sessions tab listed the store-less project at 0 sessions without breaking. One behaviour change worth flagging: StoresOverview.sessions.projectCount now counts the union, so it includes registry keys with no transcripts (3 rather than 2 in the fixture). That follows from the new definition of the project set and the dashboard label still reads 'projects', but on the owner's machine it will jump by however many registry-only keys exist. One friction, not a defect: jig's PostToolUse edit-observe guard for raw-path-across-the-seam fired on shared/contract.ts and electron/main/workspace/sessions.ts for the line 'guessedPath: string | null'. The check's own declared paths are only electron/preload/**/*.ts and electron/main/ipc.ts, and its header comment says display paths in shared/contract.ts are fine; the canonical driver (node .jig/checks/run.mjs) reports no findings on either file. The session-lane guard appears to ignore the check's path scope. Worth a /jig:review pass.\n2026-09-02 scope drift — touched but unpredicted: test/plugin-toggle.test.ts, test/sessions.test.ts, test/skill-move.test.ts, test/user-store.test.ts\n2026-09-02 lesson recorded: .foreman/notes.jsonl, area test\n2026-09-02 scope drift — touched but unpredicted: .foreman/notes.jsonl, ROADMAP.jsonl, test/plugin-toggle.test.ts, test/sessions.test.ts, test/skill-move.test.ts, test/user-store.test.ts","model":"opus","effort":"high"}
{"id":"026","title":"The Projects home: project-first navigation","why":"The first screen today is a dashboard of store directory sizes, and the sidebar is eight kind-first tabs. The owner's first requirement is that opening kondo shows projects and what is tied to each. The seam can serve it as a projection over the kinds once 023-025 land.","what":"Channels projectsList() (one row per project plus a global row, tier-1 counts of skills/agents/commands/rules/hooks/MCP servers/layers by readdir names only) and projectDetail(projectId) (tier-2 reads for the one project opened). New src/features/projects view as the initial view: project list left, project page right with sections Skills, Plugins, Hooks, Agents, MCP servers, Sessions and actions inline; plugin control per project is On here / Off here / Follows global with the layer chosen by policy (highest layer that already states a value, else settings.local.json) and the chip strip behind a disclosure. Sidebar collapses to Projects, Clean up (Tidy renamed), History (Journal renamed); the dashboard becomes a Storage section of the global row. A shared LastChange banner offers Undo where the change was made. Plan doc first.","status":"done","source":"user","depends_on":["023","024","025"],"planned_touches":["shared/contract.ts","electron/main/workspace/kinds.ts","electron/main/workspace/workspace.ts","electron/main/ipc.ts","electron/preload/index.ts","src/app/app.tsx","src/features/projects/","src/ui/","docs/plans/"],"observed_touches":["docs/adr/0007-stat-first-lazy-scanning.md","docs/adr/0008-composite-identity-ids-across-the-seam.md","docs/domain.md","docs/plans/026-projects-home.md","docs/plans/README.md","electron/main/ipc.ts","electron/main/workspace/kinds.ts","electron/main/workspace/user-store.ts","electron/main/workspace/workspace.ts","electron/preload/index.ts","shared/contract.ts","src/app/app.tsx","src/features/dashboard/dashboard.tsx","src/features/hooks/hooks.tsx","src/features/plugins/plugins.tsx","src/features/projects/plugin-control.tsx","src/features/projects/projects.tsx","src/features/sessions/sessions.tsx","src/features/settings/settings.tsx","src/features/skills/skills.tsx","src/features/tidy/tidy.tsx","src/ui/last-change.tsx","test/projects-home.test.ts"],"commits":[],"created_at":"2026-09-02","updated_at":"2026-09-02","notes":"2026-09-02 Shipped. Checks all run by me: npm run typecheck exits 0, npm run lint exits 0, npm test 169/169 across 21 files (11 new in test/projects-home.test.ts), npm run guards reports No findings. UI verified with the run-kondo skill against the synthetic fixture (X:/Temp/kondofix, never a real store): the app opened on Projects; the sidebar held exactly Projects, Clean up, History; the Global row was selected by default and carried the Storage section holding both store reports; picking the apiserver project showed Skills, Plugins, Hooks, Agents, Commands, Rules, MCP servers, Settings files and Sessions. The full plugin loop ran live - On here refused with needs-confirmation naming .claude/settings.local.json, Create it wrote it, Follows global emptied enabledPlugins to {}, Undo restored the file exactly, and journal.jsonl holds three settings-edit entries with the undo linked by undoOf. The layers disclosure opened and starred the winning layer. No unverified notes. Prompt vs reality: the brief said 023-025 already put the mcp/agent/command/rule/output-style kinds on the seam. They exist as kind definitions in kinds.ts and as DTOs in shared/contract.ts, but no KondoApi channel exposed any of them - projectDetail is the first channel that surfaces MCP servers, agents, commands, rules and output styles to the renderer. Two of the seven counts the entry named cannot come from readdir: a hook is a fragment of settings.json and an MCP server a key of ~/.claude.json or .mcp.json. ProjectRowCounts.hooks and .mcpServers are therefore null in projectsList and counted in projectDetail. Recorded as decision 1 of docs/plans/026-projects-home.md and in ADR-0007 and docs/domain.md. The listing opens no file at all once the inventory is warm - test/projects-home.test.ts proves it by wrapping fs.readFile; the only file behind projectsList is ~/.claude.json, which the project set itself comes from (ADR-0009). Two things beyond the entry planned_touches were flagged and approved before writing. First: Follows global needs a mutation that did not exist, so pluginClear(pluginId, layerId) and clearEnabledPlugin() were added (electron/main/workspace/user-store.ts). It is gated on the target layer existing disable decision rather than a new CapabilityOperation, and it is an ordinary write step, so mutations snapshots the old bytes and undo restores them with no new machinery. Second: cutting the sidebar to three left six views unreachable, so src/features/dashboard, skills, plugins, hooks, settings and sessions were deleted and their tables became the project page sections. One tooling finding: the jig raw-path-across-the-seam PostToolUse detector fired on an edit to shared/contract.ts, whose params scope it to electron/preload/**/*.ts and electron/main/ipc.ts only - the hook ignores its own paths scope and matched the field name targetPath. The field was redundant (the target layer is already in state.scopes) and was dropped, and npm run guards reports No findings, so nothing is suppressed - but the hook will keep firing on any *Path field added to the contract. Process deviation: the four Run/Expected checks all validate one indivisible diff, so there was no meaningful per-check file subset to checkpoint. One commit carries the whole change rather than four task n/4 commits.\n2026-09-02 scope drift — touched but unpredicted: docs/adr/0007-stat-first-lazy-scanning.md, docs/adr/0008-composite-identity-ids-across-the-seam.md, docs/domain.md, electron/main/workspace/user-store.ts, src/features/dashboard/dashboard.tsx, src/features/hooks/hooks.tsx, src/features/plugins/plugins.tsx, src/features/sessions/sessions.tsx, src/features/settings/settings.tsx, src/features/skills/skills.tsx, src/features/tidy/tidy.tsx, test/projects-home.test.ts\n2026-09-02 lesson recorded: .foreman/notes.jsonl, area src/features","model":"opus","effort":"high"}
{"id":"027","title":"Move a plugin between projects as one two-layer settings edit","why":"The owner asked to move plugins between projects. The matrix refuses plugin move with a reason that says kondo relocates only skills, but the native convention is not a relocation: it is an explicit false in the source layer and a true in the destination layer, which pluginTogglePlan already writes one layer at a time.","what":"pluginMove(pluginId, fromLayerId, toLayerId, createLayer?) building one settings-edit plan of two write steps (editEnabledPlugins false, then true or newSettingsSource), reusing the needs-confirmation gate for an absent destination file; flip plugin rows' move to ALLOW and reword ONLY_SKILLS_MOVE. Destination picker beside the chips (or on the project page after 026).","status":"done","source":"user","depends_on":["025"],"planned_touches":["shared/contract.ts","electron/main/workspace/kinds.ts","electron/main/workspace/capabilities.ts","electron/main/workspace/workspace.ts","electron/main/ipc.ts","electron/preload/index.ts","src/features/plugins/plugins.tsx","test/"],"observed_touches":["docs/adr/0006-native-conventions-over-invented-state.md","docs/domain.md","electron/main/ipc.ts","electron/main/workspace/capabilities.ts","electron/main/workspace/kinds.ts","electron/main/workspace/workspace.ts","electron/preload/index.ts","shared/contract.ts","src/features/projects/plugin-control.tsx","src/features/projects/projects.tsx","test/plugin-move.test.ts","ROADMAP.jsonl"],"commits":["ee8736c","5bc453e","4dd2aa6"],"created_at":"2026-09-02","updated_at":"2026-09-03","notes":"2026-09-03 Shipped and verified live. pluginMove builds ONE settings-edit plan of two write steps -- true in the destination layer, false in the source -- reusing pluginTogglePlan's own editEnabledPlugins/newSettingsSource and its needs-confirmation gate rather than a second editor or a second confirmation path. MATRIX.plugin.{user,project,local}.move is now ALLOW; the refusal the other kinds share is reworded to 'This kind does not move between scopes.' (constant ONLY_SKILLS_MOVE renamed NOTHING_TO_HAND_OVER). MutateRequest gained an optional sourceId so move still rides the generic entityMutate seam; workspace.pluginMove forwards to it exactly as skillMove does, plus a kondo:plugin-move channel, ipc line and preload line. THREE DELIBERATE DEVIATIONS FROM THE BRIEF. (1) The signature is pluginMove(pluginId, fromLayerId, destinationId, createLayer?), not toLayerId. workspace.ts projectDetail builds its KindContext narrowed to the one project -- context(c, undefined, only) -- so PluginInfo.scopes on that page holds only the user layer and that project's two; no other project's settings-layer id exists in the renderer to pass. The destination therefore travels as a scope id ('user' or project:code:<dirName>), the vocabulary skillMove already takes, and which of that scope's layers receives the true is resolved in main by the new destinationLayer(), the same rule projectPluginStates picks a toggle's target by -- which also keeps domain.md's stated principle that the target file is never the renderer's to pick. (2) Step order is destination-first, source-second, not the brief's 'false then true'. Both steps are one journal entry either way, but an interrupted run then leaves the plugin enabled in both scopes rather than in neither, which is ADR-0001's write-before-release order. (3) src/features/plugins/plugins.tsx does not exist and never did. Entry 026's projects view is in place, so the picker went beside the chips in src/features/projects/plugin-control.tsx, with the self-excluding destination list computed in projects.tsx. BEYOND THE PLANNED SURFACE, flagged to the user and approved before writing: docs/domain.md and docs/adr/0006-native-conventions-over-invented-state.md. Two jig guards block the commit otherwise -- workspace-adapter-outruns-domain-doc pairs electron/main/workspace/** with docs/domain.md, and seam-contract-outruns-its-adr pairs shared/contract.ts with docs/adr/**. Each got one factual paragraph recording that a plugin move is two enabledPlugins statements and not a relocation. CHECKS, all run here. npm run typecheck exits 0. npm test: 204 passing across 22 files, 13 of them new in test/plugin-move.test.ts (two-step plan and stepCount 2, byte-for-byte undo of both files, the absent-destination needs-confirmation writing nothing anywhere until confirmed, the created layer holding only that key, the allowed capability in all three layers, and that no kind's refusal says 'Only skills' any more). npm run lint exits 0. Live against the run-kondo fixture: on the Global page foreman's picker is live and hush's is dimmed reading 'Only a scope that turns the plugin on has one to hand over'; choosing apiserver asked to create settings.local.json and wrote nothing until 'Create it', then produced one journal entry with two write steps -- foreman@acme false in the user settings.json, true in the project's settings.local.json, the source's previous bytes in the trash. Undo put the user layer back to true and removed the created file. A project-to-project move (apiserver -> website) behaved identically, and each page excludes itself from its own destination list. No unverified: lines -- every check ran. HARNESS GAP worth fixing: .claude/skills/run-kondo/drive.mjs 'pick' matches only 'tbody tr', so it cannot reach the plugin rows, which are divs. I drove the select through 'eval' with the same native-setter recipe the skill documents. Generalising pick's row selector past tbody tr would make the documented command work for the plugins section.\n2026-09-03 lesson not recorded (no_observed_paths): electron/main/workspace/workspace.ts projectDetail narrows its KindContext to the opened project (context(c, undefined, only)), so PluginInfo.scopes there covers only the user layer and that project's own -- another project's settings-layer id never reaches the renderer, which is why a cross-scope plugin destination has to travel as a scope id.\n2026-09-03 scope drift — predicted but untouched: src/features/plugins/plugins.tsx, test/; touched but unpredicted: docs/adr/0006-native-conventions-over-invented-state.md, docs/domain.md, src/features/projects/plugin-control.tsx, src/features/projects/projects.tsx\n2026-09-03 scope drift — predicted but untouched: shared/contract.ts, electron/main/workspace/kinds.ts, electron/main/workspace/capabilities.ts, electron/main/workspace/workspace.ts, electron/main/ipc.ts, electron/preload/index.ts, src/features/plugins/plugins.tsx\n2026-09-03 scope drift — predicted but untouched: shared/contract.ts, electron/main/workspace/kinds.ts, electron/main/workspace/capabilities.ts, electron/main/workspace/workspace.ts, electron/main/ipc.ts, electron/preload/index.ts, src/features/plugins/plugins.tsx, test/; touched but unpredicted: ROADMAP.jsonl","model":"opus"}
{"id":"028","title":"Promote agents, commands, rules and output styles between scopes","why":"The owner asked to move an agent or similar from local to global. The skill move recipe (copy -> verify -> trash, one plan) is exactly that promotion, and digestTree already verifies single files; only discovery (024), a parameterised placement and matrix rows are missing.","what":"Generalise skillPlacement into a per-kind placement table, allow move for user-placed agents/commands/rules/output-styles, and expose the move through the generic mutate channel of entry 035 (or an entityMove(id, destinationId) if 035 has not landed). Same collision refusal, same undo.","status":"planned","source":"user","depends_on":["024","035"],"planned_touches":["electron/main/workspace/kinds.ts","electron/main/workspace/capabilities.ts","electron/main/workspace/user-store.ts","shared/contract.ts","test/"],"observed_touches":[],"commits":[],"created_at":"2026-09-02","updated_at":"2026-09-02","notes":""}
{"id":"029","title":"Read skillOverrides and decide the per-skill disable convention","why":"ADR-0006 names skillOverrides as a toggle surface and nothing reads it. It is Claude's documented per-skill switch, reaches plugin-shipped skills, applies per layer, and the owner's settings.json holds nine entries — so kondo shows a skill as enabled that Claude has switched off. The project-scope skills.disabled kondo writes is also unobserved in the wild.","what":"Read skillOverrides across layers (local > project > user) in readSettingsLayers; add override and effective state to SkillInfo; drive the matrix decision from the effective state and name the overriding layer in refusals. Verify project-scope skills.disabled against Claude Code once; record which convention each scope writes in ADR-0006 and domain.md. If a settings-edit disable is chosen, generalise editEnabledPlugins to a member name.","status":"planned","source":"user","depends_on":["025"],"planned_touches":["electron/main/workspace/user-store.ts","electron/main/workspace/kinds.ts","electron/main/workspace/capabilities.ts","shared/contract.ts","docs/adr/0006-native-conventions-over-invented-state.md","docs/domain.md","test/"],"observed_touches":[],"commits":[],"created_at":"2026-09-02","updated_at":"2026-09-02","notes":""}
{"id":"030","title":"Tidy categories for dead and scratch project directories","why":"The owner wants projects no longer developed and temporary projects found. With entry 022 a registry path that no longer stats is a dead project (52 on the owner's machine), and 9,031 of 9,171 directories sat under a temp directory; today the only sweep signal is per-transcript mtime.","what":"Distinguish unlocated from gone on ProjectRecord/SessionProject. Add tidy categories dead-projects (registry path missing) and scratch-projects (flattened name under os.tmpdir(), --claude-worktrees-, --claude-jobs-, memory-only or transcript-less directories) whose candidates are whole projects/<flat> trash steps in the user store, with the exclusivity rule that no path sits under two categories. Relabel the Clean up rows in user words.","status":"planned","source":"user","depends_on":["022","025"],"planned_touches":["electron/main/workspace/analysis.ts","electron/main/workspace/tidy.ts","electron/main/workspace/sessions.ts","shared/contract.ts","src/features/tidy/tidy.tsx","test/tidy.test.ts","docs/glossary.md"],"observed_touches":[],"commits":[],"created_at":"2026-09-02","updated_at":"2026-09-02","notes":""}
{"id":"031","title":"Configuration orphans: a splice step and cleanup of ~/.claude.json and settings keys","why":"The owner named MCP declarations tied to no project. They live in ~/.claude.json, a 2 MB file Claude rewrites during every session; the write step replaces a whole file with bytes planned from a scan and undo restores a whole snapshot, so either would discard Claude's concurrent changes (ADR-0009 consequences). The splicer also cannot remove a member.","what":"Own ADR: a splice PlannedStep {store, at, expectDigest, edits} that re-reads and refuses on a digest mismatch, journals the edits, and undoes by inverse splice on current bytes; a user-config store root for ~/.claude.json in the locator and mutations; editMember(source, keyPath, literal | null) generalising editEnabledPlugins with removal. Then configOrphansPreview/Remove: project entries and MCP declarations for missing paths, enabledPlugins keys for uninstalled plugins (also listed as ghost rows), skillOverrides for missing skills.","status":"done","source":"user","depends_on":["023"],"planned_touches":["electron/main/workspace/mutations.ts","electron/main/workspace/user-store.ts","electron/main/workspace/locator.ts","electron/main/workspace/kinds.ts","shared/contract.ts","docs/adr/","test/mutation.test.ts"],"observed_touches":["docs/adr/0009-projects-come-from-claudes-registry.md","docs/adr/0010-splice-config-files-never-whole-file-writes.md","docs/adr/README.md","docs/domain.md","electron/main/ipc.ts","electron/main/workspace/kinds.ts","electron/main/workspace/locator.ts","electron/main/workspace/mutations.ts","electron/main/workspace/user-store.ts","electron/main/workspace/workspace.ts","electron/preload/index.ts","shared/contract.ts","test/mutation.test.ts"],"commits":["1b614f9","132fc15"],"created_at":"2026-09-02","updated_at":"2026-09-02","notes":"2026-09-02 Shipped. npm run typecheck exits 0; npm test 183 passed across 21 files (test/mutation.test.ts 15 -> 29); npm run lint exits 0; npm run guards reports no findings. ADR-0010 (docs/adr/0010-splice-config-files-never-whole-file-writes.md) and a splice PlannedStep in electron/main/workspace/mutations.ts: it re-reads at apply time, refuses a digest mismatch with the new stale-file ScanErrorCode, journals edits plus their inverse (undoEdits), and undoes by applying that inverse to the file current bytes. Edits are sequential - each offset is measured against the result of the ones before it - which is what lets two adjacent JSON members come out in one step with no overlap arithmetic. The digest is checked twice: at plan time so the ordinary concurrent-write case refuses without journaling, and again at apply time, which is the check that counts. The splice writes through a temporary sibling renamed over the target, so a 2 MB registry write cannot tear. That temp has to share a filesystem with the file, so for ~/.claude.json it lives in the home directory for the length of one step; the write-boundary test in test/mutation.test.ts pins it as the one path a splice touches outside a scanned store, and asserts it is gone afterwards. locator.ts gained userConfigRoot; mutations.ts registers user-config as a store whose only permitted member is .claude.json, and it deliberately sits outside the ADR-0001 nested check - kondo data root normally lives under the same home, which would otherwise refuse every mutation on a real machine. editEnabledPlugins and clearEnabledPlugin are now wrappers over editMember(source, keyPath, literal | null) in user-store.ts; behaviour is byte-identical (the 17-case plugin-toggle suite is unchanged and green). configOrphansPreview / configOrphansRemove cover the four kinds asked for. Dead registry entries are named from the session inventory pathExists that ADR-0009 already paid for, so the scan costs no new stat. Only the object form of enabledPlugins yields a removable row - a key inside the legacy array form would need the array reformatted, which ADR-0005 forbids. Ghost rows: an uninstalled enabledPlugins key is a PluginInfo with installed:false in pluginsList. It is deliberately excluded from projectPluginStates - the per-project three-way control has nothing to turn on for a plugin that is not there, and the renderer has no installed marker to tell one apart, so an included row would have read as installed. Acting on it goes through the orphan preview. Beyond the planned file surface, all forced rather than chosen: electron/preload/index.ts (it builds a full KondoApi object, so a new method must be wired or typecheck fails), electron/main/ipc.ts (the channel), electron/main/workspace/workspace.ts (the KondoApi implementation), and docs/domain.md plus docs/adr/0009 (both stated in prose that kondo cannot write ~/.claude.json, which this entry makes false). Open, deliberately not built here: no renderer surface exists for configOrphansPreview / configOrphansRemove - they are reachable over IPC only, so there was nothing to look at and no unverified line to record. capabilities.ts MCP_IS_READ_ONLY still refuses every MCP operation; the splice it named as its blocker now exists, so that row is unblocked, but writing an MCP declaration was not in this entry scope. Checkpointed on main rather than a foreman/ branch, matching this repo recent history (23eb25e, bf5579c and the task n/3 commits are all on main). Eight working copies carried CRLF while the index is LF (core.autocrlf=input); the ones this entry touched were normalized, with no content diff.\n2026-09-02 scope drift — predicted but untouched: test/mutation.test.ts; touched but unpredicted: docs/domain.md, electron/main/ipc.ts, electron/main/workspace/workspace.ts, electron/preload/index.ts\n2026-09-02 lesson recorded: .foreman/notes.jsonl, area electron/main\n2026-09-02 scope drift — predicted but untouched: electron/main/workspace/mutations.ts, electron/main/workspace/user-store.ts, electron/main/workspace/locator.ts, electron/main/workspace/kinds.ts, shared/contract.ts, docs/adr/","model":"opus","effort":"high"}
{"id":"032","title":"Duplicate skills across scopes, with a trash operation","why":"The owner wants duplicates found. Same-name skills exist across scopes on the owner's machine, scanSkills already lists them all, and digestTree already hashes a skill tree but is private to the copy verifier. No operation can trash one redundant copy.","what":"skillDuplicates() grouping by name over the existing listing with a digest per member of groups larger than one (move digestTree to scan.ts); add trash to CapabilityOperation for user-placed skill scopes and a skillTrash(skillId) plan of one trash step. Surface never-used skills from ~/.claude.json skillUsage as a badge.","status":"planned","source":"user","depends_on":["025"],"planned_touches":["electron/main/workspace/scan.ts","electron/main/workspace/mutations.ts","electron/main/workspace/kinds.ts","electron/main/workspace/capabilities.ts","shared/contract.ts","src/features/skills/skills.tsx","test/"],"observed_touches":[],"commits":[],"created_at":"2026-09-02","updated_at":"2026-09-02","notes":""}
{"id":"033","title":"Tidy categories for plugin residue and orphan session-env directories","why":"domain.md names session-env as an orphan-sweep candidate and the RECLAIMABLE allowlist cannot name it; 5,213 session-env directories against 11,686 transcripts. Under plugins/, 28 of 39 cached versions were superseded and 47 of 55 data directories matched no installed plugin.","what":"Categories orphan-session-env (directories whose uuid matches no transcript), superseded-plugin-versions (cache version dirs that are not the installed installPath) and orphan-plugin-residue (.install-manifests and data/ entries for ids absent from installed_plugins.json), each a trash step in the user store. Never candidate the installed installPath.","status":"planned","source":"user","depends_on":[],"planned_touches":["electron/main/workspace/tidy.ts","electron/main/workspace/user-store.ts","shared/contract.ts","src/features/tidy/tidy.tsx","test/tidy.test.ts","docs/domain.md"],"observed_touches":[],"commits":[],"created_at":"2026-09-02","updated_at":"2026-09-02","notes":""}
{"id":"034","title":"Duplicate sessions: mirrored across stores and near-duplicate first prompts","why":"README, the glossary and domain.md promise duplicate detection and nothing computes it. The same-id half is tier-1 over listings already made; the same-prompt half is tier-2 and needs the ADR-0007 cache that does not exist yet.","what":"SessionSummary.mirroredIn from the desktop listing (local_<uuid> stems). sessionNearDuplicates(projectId) streaming first prompts for one project, backed by a scan-cache.ts keyed (path, size, mtime) in <kondo-data> as ADR-0007 decided; sessionTrash(ids) as one trash plan. Amend ADR-0007 back to present tense when the cache lands.","status":"planned","source":"user","depends_on":[],"planned_touches":["electron/main/workspace/workspace.ts","electron/main/workspace/kinds.ts","electron/main/workspace/jsonl.ts","electron/main/workspace/scan-cache.ts","shared/contract.ts","src/features/sessions/sessions.tsx","docs/adr/0007-stat-first-lazy-scanning.md","test/"],"observed_touches":[],"commits":[],"created_at":"2026-09-02","updated_at":"2026-09-02","notes":""}
{"id":"035","title":"One plan seat in the registry and a generic mutate channel","why":"The registry's enable(entity)/disable(entity) seats cannot carry a destination or a settings layer, so the skill move and plugin toggle live as standalone planners with their own workspace method, channel, IPC line and preload line each. The kinds the vision needs (mcp, agent, command, rule, output-style) with toggle and move would add around fifteen more channels.","what":"Replace the two seats with plan(entity, request, context) => PlanResult where request is {op: enable|disable|move, targetId?, confirm?}; move skillPlan, skillMovePlan and pluginTogglePlan into their kinds; one entityMutate(entityId, request) workspace method dispatching on the id's kind prefix, and entityList(kind, parentId?) for the new kinds; keep the shipped channels as thin aliases. Delete the unused scopes and capabilities(scope) seats. Amend ADR-0004 (the unit of contract growth is the operation, not the kind).","status":"done","source":"user","depends_on":[],"planned_touches":["electron/main/workspace/kinds.ts","electron/main/workspace/workspace.ts","shared/contract.ts","electron/main/ipc.ts","electron/preload/index.ts","docs/adr/0004-electron-context-isolated-bridge.md","test/kinds.test.ts"],"observed_touches":["docs/adr/0004-electron-context-isolated-bridge.md","docs/domain.md","electron/main/ipc.ts","electron/main/workspace/kinds.ts","electron/main/workspace/workspace.ts","electron/preload/index.ts","shared/contract.ts","test/kinds.test.ts","test/placed.test.ts"],"commits":["b527a41","333193a"],"created_at":"2026-09-02","updated_at":"2026-09-02","notes":"2026-09-02 scope drift — predicted but untouched: test/kinds.test.ts; touched but unpredicted: docs/domain.md\n2026-09-02 Done and verified with all three checks green: `npm run typecheck` (tsc -b, exit 0), `npm test` (21 files, 189 tests passing), `npm run lint` (oxlint, exit 0). What changed. electron/main/workspace/kinds.ts: EntityKindDefinition lost `scopes`, `capabilities(scope)`, `enable` and `disable`, and gained one `plan(entity, request, context) => PlanResult`. skillPlan became skillTogglePlan, skillMovePlan and pluginTogglePlan moved into skill.plan and plugin.plan and are no longer exported; `noPlanYet` is gone, replaced by `matrixRefusal(kind, scope, op)` so every read-only kind refuses in the capability matrix's own words instead of returning a bare null. Added an exported `listings` table (idPrefix, kind, parent, definition) with `listingForId` (longest prefix wins, so `skill:plugin/` beats `skill:` and `session:desktop:` is distinct from `session:code:`) and `listingFor(kind, parentId?)`. workspace.ts gained `entityMutate(entityId, request)` and `entityList(kind, parentId?)`; skillsList, skillToggle, skillMove, pluginsList, pluginSkills, pluginToggle, hooksList, settingsLayers, desktopSessions, sessionList and sessionProjects are now aliases over them, each keeping its own id-shape guard so its error messages are unchanged. shared/contract.ts gained `MutateRequest` and the two methods plus their channels; ipc.ts and preload/index.ts gained one line each. `storelessDestination` moved from workspace.ts into kinds.ts so the generic channel gives the same refusal the skillMove alias always did. Two deliberate departures from the brief, both flagged to the user first. (1) docs/domain.md is in the diff: jig's `workspace-adapter-outruns-domain-doc` guard blocks any commit touching electron/main/workspace/** without it. This refactor changes no on-disk store fact, so on the user's instruction the paired edit is a real correction rather than a filler line - docs/domain.md:112-120 still said the MCP matrix refuses writes 'until entry 031 ships a write path', but 031 shipped and was accepted while the row is still read-only, so the text now says the splice step exists and nothing has wired an MCP toggle to it. (2) test/placed.test.ts is in the diff too: it asserted on the deleted `kinds.agent.enable/disable` seats and had to move to the plan seat. Nothing else outside the expected surface was touched. pluginClear is NOT an alias over entityMutate and keeps its own channel. `clear` is not one of the three values `CapabilityOperation` holds, and the brief pins the request shape to enable|disable|move; the matrix has no `clear` row to gate it on. kinds.ts:pluginClearPlan documents why. One performance note: KindContext gained a memoized `skills()` alongside `layers()` and `plugins()`. A move previously took two skill scans (one to find the entity, one for the collision check); it now takes one, and configOrphans reuses the same listing. Verified in the running app, not only in vitest - vitest calls the workspace directly and never crosses ipcMain or contextBridge, which is exactly what this entry changed. Ran the run-kondo skill against the synthetic fixture at X:/Temp/kondofix (never a real ~/.claude). `window.kondo.entityList('skill')` returned the same four ids as `skillsList()`; `entityMutate` on a settings layer answered `not-permitted` with 'A settings layer is a file, not a toggle.'; an id with an unclaimed prefix answered `bad-request`; `pluginSkills` returned the plugin's own skill through the alias. Pressed Disable on a skill in the real window: the journal recorded one reversible move step, the directory moved on disk, the view re-read and offered Undo, and journalUndo put it back. Projects home, Clean up and History all rendered with no errors in the main-process log. No unverified checks remain.\n2026-09-02 scope drift — predicted but untouched: electron/main/workspace/kinds.ts, electron/main/workspace/workspace.ts, shared/contract.ts, electron/main/ipc.ts, electron/preload/index.ts, docs/adr/0004-electron-context-isolated-bridge.md; touched but unpredicted: test/placed.test.ts\n2026-09-02 lesson recorded: .foreman/notes.jsonl, area electron/main","model":"opus","effort":"high"}
{"id":"036","title":"Hooks per project, script health, and hook move between layers","why":"Hooks are a flat table with no project column; a command naming a missing script and a script armed by no hook are both invisible (the owner's user store holds two scripts and an empty hooks object). Hook enable/disable is rightly refused (no native convention) but move between layers is a two-layer settings edit the refusal text calls impossible.","what":"Group hooks by projectId (025). HookInfo.script {path, status: present|missing|unverifiable} statting only paths inside a store or a verified .claude (ADR-0002). Tidy category unarmed-hook-scripts. Give hook rows a distinct move reason now; when built, plan the move as remove-group plus insert-group (entry 031 splice), refusing commands that reference $CLAUDE_PROJECT_DIR or a .claude/hooks relative path.","status":"planned","source":"user","depends_on":["025","031"],"planned_touches":["electron/main/workspace/analysis.ts","electron/main/workspace/user-store.ts","electron/main/workspace/capabilities.ts","electron/main/workspace/tidy.ts","shared/contract.ts","src/features/hooks/hooks.tsx","test/"],"observed_touches":[],"commits":[],"created_at":"2026-09-02","updated_at":"2026-09-02","notes":""}
{"id":"037","title":"UI words, empty states and inline refusal reasons","why":"The owner's first word was simple to use. Labels a first-time user meets — Journal, sweep, dry run, sidecar, orphan, scope, layer, winning layer, unrecognized entries — are kondo internals; refusal reasons live only in hover tooltips; empty lists render a bare table header.","what":"Add a UI-words column to docs/glossary.md and apply it (History, Clean up, move to trash, preview, session folder, untouched 30+ days, settings file, in effect). AsyncView gains an empty prop and a Refreshing pill; denied controls render their reason as a caption; mutation results join every error message. Demote the Problems banner to muted text when it holds only unknown entries.","status":"planned","source":"user","depends_on":["026"],"planned_touches":["docs/glossary.md","src/ui/async-view.tsx","src/ui/problems.tsx","src/features/","electron/main/workspace/capabilities.ts"],"observed_touches":[],"commits":[],"created_at":"2026-09-02","updated_at":"2026-09-02","notes":""}
{"id":"038","title":"Separate the project count from the transcript-bearing project count on the dashboard","why":"Entry 025 redefined the project set as the union of the ~/.claude.json registry keys and the ~/.claude/projects directories. StoresOverview.sessions.projectCount in electron/main/workspace/workspace.ts is still scan.data.projects.length, so it now counts registry keys with no transcripts too — in the run-kondo fixture it read 3 where 2 projects have sessions. The dashboard card still labels it plain projects, so on a real store the number will jump by however many registered-but-unworked or dead keys exist and read as a regression rather than a wider definition.","what":"Decide what the dashboard should show and split the figure accordingly: keep projectCount as the union size and add a second count (or a sub-line) for the members with transcripts, using the sources/pathExists/hasStore fields SessionProject already carries. Touches StoresOverview in shared/contract.ts, storesOverview() in electron/main/workspace/workspace.ts, src/features/dashboard/dashboard.tsx, and the affected rows of docs/domain.md.","status":"planned","source":"claude-suggested","depends_on":["025"],"planned_touches":["shared/contract.ts","electron/main/workspace/workspace.ts","src/features/dashboard/dashboard.tsx","docs/domain.md"],"observed_touches":[],"commits":[],"created_at":"2026-09-02","updated_at":"2026-09-02","notes":""}
{"id":"039","title":"Scope the raw-path-across-the-seam PostToolUse detector to its own paths","why":"The guard module in .jig scopes itself to electron/preload/**/*.ts and electron/main/ipc.ts and its own comment says display paths in shared/contract.ts are fine. The PostToolUse detector ignores that paths param and fires on any file: during entry 026 it blocked an Edit to shared/contract.ts over a targetPath: string field. npm run guards is correct, so this costs only wasted round-trips and pressure to misname contract fields.","what":"In the raw-path-across-the-seam check module, make the PostToolUse detector honour the same params.paths glob the checks runner uses, so an edit outside electron/preload/** and electron/main/ipc.ts is not matched. Verify by adding a *Path display field to shared/contract.ts and confirming no hook fires while npm run guards still reports No findings.","status":"planned","source":"claude-suggested","depends_on":[],"planned_touches":[],"observed_touches":[],"commits":[],"created_at":"2026-09-02","updated_at":"2026-09-02","notes":""}
{"id":"040","title":"A view for configuration orphans","why":"Entry 031 shipped configOrphansPreview and configOrphansRemove on KondoApi (shared/contract.ts) with channels, ipc.ts and preload wiring, but no renderer surface: the only way to reach them today is over IPC. The dead registry entries, ghost enabledPlugins keys and stale skillOverrides they find are invisible in the app.","what":"A view listing ConfigOrphan rows grouped by kind (project-entry, mcp-declaration, enabled-plugin, skill-override), each showing name, source and reason, with a multi-select and one Remove that calls configOrphansRemove and then re-reads. It must render the stale-file ScanErrorCode as its own message — the file moved on, nothing was written, re-read and try again — rather than as a generic failure, since that refusal is the expected outcome whenever Claude has written ~/.claude.json in between (ADR-0010). Likely files: src/features/, src/app/app.tsx, and PluginInfo.installed if the plugins listing ever gets a view that must mark a ghost row.","status":"planned","source":"claude-suggested","depends_on":[],"planned_touches":[],"observed_touches":[],"commits":[],"created_at":"2026-09-02","updated_at":"2026-09-02","notes":""}
{"id":"041","title":"run-kondo SKILL.md Drive section names a Skills tab the app no longer has","why":"Verified live on 2026-09-03 while working entry 020: the running app reports nav buttons [\"Projects\",\"Clean up\",\"History\"], and src/app/app.tsx declares exactly those three in its views array. SKILL.md still teaches `node drive.mjs open Skills` and `node drive.mjs pick commit-writer apiserver` as its two worked examples, so the first command a reader copies cannot match a tab and the second describes reaching a skill through a view that was replaced. plugin-control.tsx line 17 already calls the old arrangement \"the old plugins view\" in its own comment. The skill is the project harness every UI check runs through, so a stale example costs a round-trip on every use.","what":"Rewrite the Drive section of .claude/skills/run-kondo/SKILL.md against the current app. Skills and plugins are now sections inside the Projects view (src/features/projects/projects.tsx, Section titles Skills and Plugins), not sidebar tabs, so `open Skills` has to become whatever reaches the Global or a project row in the left list. Check drive.mjs open still works at all for Projects/Clean up/History, and re-check whether `pick` finds a move select now that the rows live under a project detail. Also decide what to say about pluginSkills: it is on the preload bridge (window.kondo.pluginSkills) and covered by test/plugin-skills.test.ts, but no renderer consumes it, so the only way to see its answer today is drive.mjs eval across the seam - which is how entry 020 verified the ships-no-skills fixture. Verify by launching against the fixture and running every command the page prints.","status":"planned","source":"claude-suggested","depends_on":[],"planned_touches":[],"observed_touches":[],"commits":[],"created_at":"2026-09-03","updated_at":"2026-09-03","notes":""}
{"id":"042","title":"Re-arm all four PostToolUse guards once jig evalEdit honours params.paths","why":"After entries 017 and 021 every guard in .jig/config.json sits in mode observe (renderer-reaches-past-the-bridge, test-touches-a-real-store, raw-path-across-the-seam, outbound-network-call), so kondo has no armed session-lane guard at all. That is the correct state while jig 2.7.1 hooks/jig-lib.js evalEdit (line 623) reads params.patterns but never params.paths, but it leaves the pre-commit hook and .github/workflows/ci.yml line 30 as the only enforcement; if either is bypassed the invariants are unguarded.","what":"Blocked on an upstream fix in the slag repo: evalEdit must filter on det.params.paths the way .jig/checks/run.mjs already does (matchesAny against p.paths). Once a jig release ships that, verify it by driving jig-lib's evaluateGuard and effectiveState directly against an out-of-scope file (e.g. electron/main/workspace/workspace.ts with a node:fs import) and confirming the guard no longer fires at all, then flip all four mode fields in .jig/config.json back to armed. Do not edit any check file - all carry proof hashes in .jig/config.json. Verify with node .jig/checks/run.mjs, node .jig/checks/run.mjs --selftest, npm test and npm run lint.","status":"planned","source":"claude-suggested","depends_on":["017","021"],"planned_touches":[".jig/config.json"],"observed_touches":[],"commits":[],"created_at":"2026-09-03","updated_at":"2026-09-03","notes":""}
{"id":"043","title":"drive.mjs pick reaches only table rows","why":"The run-kondo skill documents `node drive.mjs pick <row> <option>` as the way to choose a move destination, but its implementation matches only `tbody tr`. The plugins section of the project page renders each row as a div, so `pick foreman apiserver` answers `no row containing foreman` and the documented command cannot drive the plugin move control entry 027 shipped. Verifying it needed a hand-written eval with the native-setter recipe instead.","what":"In .claude/skills/run-kondo/drive.mjs, widen the pick command row selector past `tbody tr` so it also matches the div rows of the project page plugins section — anything holding a <select> whose text contains the row argument. Keep the native-setter plus bubbling change event, which is the part React needs. Update the SKILL.md line that names pick if the vocabulary changes.","status":"planned","source":"claude-suggested","depends_on":[],"planned_touches":[],"observed_touches":[],"commits":[],"created_at":"2026-09-03","updated_at":"2026-09-03","notes":""}
